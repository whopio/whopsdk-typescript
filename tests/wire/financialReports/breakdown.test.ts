// This file was auto-generated by Fern from our API Definition.

import * as Whop from "../../../src/api/index";
import { WhopClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("BreakdownClient", () => {
    test("retrieve (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            activity_filters: { direction: "money_in", line_types: ["platform_balance_transfer_outgoing"] },
            bucket: "transfers",
            currency: "usd",
            direction: "money_in",
            items: [
                {
                    amount: { amount: "-2.50", currency: "usd", decimals: 2, display_decimals: 2 },
                    avatar: { shape: "circle", url: null },
                    image_url: "image_url",
                    key: "key",
                    name: "name",
                    object: "user",
                    resource_id: "resource_id",
                },
            ],
            other_amount: { amount: "-2.50", currency: "usd", decimals: 2, display_decimals: 2 },
            other_name: "Other",
        };

        server
            .mockEndpoint()
            .get("/financial_reports/breakdown")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.financialReports.breakdown.retrieve({
            account_id: "account_id",
            bucket: "transfers",
            direction: "money_in",
            currency: "currency",
            from: "2024-01-15T09:30:00Z",
            to: "2024-01-15T09:30:00Z",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("retrieve (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/financial_reports/breakdown")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.financialReports.breakdown.retrieve({
                account_id: "account_id",
                bucket: "transfers",
                direction: "money_in",
                currency: "currency",
                from: "2024-01-15T09:30:00Z",
                to: "2024-01-15T09:30:00Z",
            });
        }).rejects.toThrow(Whop.BadRequestError);
    });

    test("retrieve (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/financial_reports/breakdown")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.financialReports.breakdown.retrieve({
                account_id: "account_id",
                bucket: "transfers",
                direction: "money_in",
                currency: "currency",
                from: "2024-01-15T09:30:00Z",
                to: "2024-01-15T09:30:00Z",
            });
        }).rejects.toThrow(Whop.UnauthorizedError);
    });

    test("retrieve (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/financial_reports/breakdown")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.financialReports.breakdown.retrieve({
                account_id: "account_id",
                bucket: "transfers",
                direction: "money_in",
                currency: "currency",
                from: "2024-01-15T09:30:00Z",
                to: "2024-01-15T09:30:00Z",
            });
        }).rejects.toThrow(Whop.ForbiddenError);
    });
});
