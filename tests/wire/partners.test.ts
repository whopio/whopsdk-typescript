// This file was auto-generated by Fern from our API Definition.

import * as Whop from "../../src/api/index";
import { WhopClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PartnersClient", () => {
    test("create (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            referral_link: "https://whop.com/network/",
            whop_partner_enabled_at: "2026-01-01T12:00:00Z",
        };

        server.mockEndpoint().post("/partners").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.partners.create();
        expect(response).toEqual(rawResponseBody);
    });

    test("create (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().post("/partners").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.partners.create();
        }).rejects.toThrow(Whop.UnauthorizedError);
    });

    test("create (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().post("/partners").respondWith().statusCode(403).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.partners.create();
        }).rejects.toThrow(Whop.ForbiddenError);
    });

    test("create (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { error: { message: "message", type: "type" } };

        server.mockEndpoint().post("/partners").respondWith().statusCode(409).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.partners.create();
        }).rejects.toThrow(Whop.ConflictError);
    });

    test("leaderboard (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            leaders: [
                {
                    first_referral_started_at: "2026-01-01T12:00:00Z",
                    rank: 1,
                    total_earnings_usd: "100.00",
                    total_volume_usd: "0.00",
                    user: {
                        city: "Austin",
                        country: "US",
                        id: "user_xxxxxxxxxxxxxx",
                        name: "Marcus Webb",
                        profile_picture: { url: "https://ui-avatars.com/api/" },
                        username: "marcuswebb",
                    },
                },
            ],
            me: {
                first_referral_started_at: "2026-01-01T12:00:00Z",
                rank: 2,
                total_earnings_usd: "4.00",
                total_volume_usd: "40.00",
                user: {
                    city: "Austin",
                    country: "US",
                    id: "user_xxxxxxxxxxxxxx",
                    name: "Marcus Webb",
                    profile_picture: { url: "https://ui-avatars.com/api/" },
                    username: "marcuswebb",
                },
            },
        };

        server
            .mockEndpoint()
            .get("/partners/leaderboard")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.partners.leaderboard();
        expect(response).toEqual(rawResponseBody);
    });

    test("leaderboard (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/partners/leaderboard")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.partners.leaderboard();
        }).rejects.toThrow(Whop.BadRequestError);
    });

    test("referredUsers (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: [
                {
                    total_earnings_usd: "10.00",
                    total_volume_usd: "100.00",
                    user: {
                        city: "Austin",
                        country: "US",
                        id: "user_xxxxxxxxxxxxxx",
                        name: "Tanya Cole",
                        profile_picture: { url: "https://ui-avatars.com/api/" },
                        username: "tanyacole",
                    },
                },
            ],
            page_info: {
                end_cursor: "WyJjdXJzb3IiLDFd",
                has_next_page: false,
                has_previous_page: false,
                start_cursor: "WyJjdXJzb3IiLDFd",
            },
        };

        server
            .mockEndpoint({ once: false })
            .get("/partners/referred_users")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.partners.referredUsers();

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("referredUsers (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/partners/referred_users")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.partners.referredUsers();
        }).rejects.toThrow(Whop.UnauthorizedError);
    });
});
