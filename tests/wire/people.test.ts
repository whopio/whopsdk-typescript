// This file was auto-generated by Fern from our API Definition.

import * as Whop from "../../src/api/index";
import { WhopClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PeopleClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: [
                {
                    account_id: "biz_xxxxxxxxxxxxxx",
                    aov: 74.99,
                    device: { browser: "Dana Whitfield", device: "Dana Whitfield", os: "Dana Whitfield" },
                    email: "marcus@shinetime.example",
                    event_count: 42,
                    first_purchase_at: "2026-01-01T12:00:00Z",
                    first_seen_at: "2026-01-01T12:00:00Z",
                    first_source: {
                        ad: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                        ad_group: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                        campaign: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                        domain: "Dana Whitfield",
                        occurred_at: "2026-01-01T12:00:00Z",
                        platform: "Dana Whitfield",
                        type: "ad_click",
                        utm_source: "Dana Whitfield",
                    },
                    id: "prsn_123",
                    last_ip: "Dana Whitfield",
                    last_purchase_at: "2026-01-01T12:00:00Z",
                    last_seen_at: "2026-01-01T12:00:00Z",
                    last_source: {
                        ad: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                        ad_group: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                        campaign: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                        domain: "Dana Whitfield",
                        occurred_at: "2026-01-01T12:00:00Z",
                        platform: "Dana Whitfield",
                        type: "ad_click",
                        utm_source: "Dana Whitfield",
                    },
                    location: { city: "Dana Whitfield", continent: "Dana Whitfield", country: "Dana Whitfield" },
                    ltv: 149.98,
                    member: {
                        id: "mem_dana1",
                        joined_at: "2026-01-01T12:00:00Z",
                        status: "Dana Whitfield",
                        usd_total_spend: 149.98,
                    },
                    name: "Dana Whitfield",
                    phone: "+xxxxxxxxxxx",
                    purchase_count: 2,
                    timezone: "Dana Whitfield",
                    user: {
                        id: "user_dana1",
                        name: "Dana Whitfield",
                        profile_pic_url: "Dana Whitfield",
                        username: "danawhitfield",
                    },
                },
            ],
            page_info: {
                end_cursor: "cursor_end",
                has_next_page: false,
                has_previous_page: false,
                start_cursor: "cursor_start",
            },
            total_count: 42,
        };

        server
            .mockEndpoint({ once: false })
            .get("/people")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.people.list({
            source: ["direct"],
            event_name: ["payment.completed"],
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/people").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.people.list();
        }).rejects.toThrow(Whop.BadRequestError);
    });

    test("list (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/people").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.people.list();
        }).rejects.toThrow(Whop.UnauthorizedError);
    });

    test("retrieve (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            account_id: "biz_xxxxxxxxxxxxxx",
            aov: 74.99,
            audience_ids: ["Dana Whitfield"],
            custom_event_names: ["Dana Whitfield"],
            device: { browser: "Dana Whitfield", device: "Dana Whitfield", os: "Dana Whitfield" },
            email: "marcus@shinetime.example",
            emails: ["Dana Whitfield"],
            event_count: 42,
            event_names: ["Dana Whitfield"],
            first_purchase_at: "2026-01-01T12:00:00Z",
            first_seen_at: "2026-01-01T12:00:00Z",
            first_source: {
                ad: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                ad_group: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                campaign: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                domain: "Dana Whitfield",
                occurred_at: "2026-01-01T12:00:00Z",
                platform: "Dana Whitfield",
                type: "ad_click",
                utm_source: "Dana Whitfield",
            },
            id: "prsn_123",
            last_ip: "Dana Whitfield",
            last_purchase_at: "2026-01-01T12:00:00Z",
            last_seen_at: "2026-01-01T12:00:00Z",
            last_source: {
                ad: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                ad_group: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                campaign: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                domain: "Dana Whitfield",
                occurred_at: "2026-01-01T12:00:00Z",
                platform: "Dana Whitfield",
                type: "ad_click",
                utm_source: "Dana Whitfield",
            },
            location: { city: "Dana Whitfield", continent: "Dana Whitfield", country: "Dana Whitfield" },
            ltv: 149.98,
            member: {
                id: "mem_dana1",
                joined_at: "2026-01-01T12:00:00Z",
                status: "Dana Whitfield",
                usd_total_spend: 149.98,
            },
            name: "Dana Whitfield",
            names: ["Dana Whitfield"],
            phone: "+xxxxxxxxxxx",
            phones: ["Dana Whitfield"],
            purchase_count: 2,
            purchases: [{ event_id: "evt_2", occurred_at: "2026-01-01T12:00:00Z", usd_value: 74.99 }],
            roles: ["Dana Whitfield"],
            sources: [
                {
                    ad: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                    ad_group: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                    campaign: { id: "adcamp_1", name: "Dana Whitfield", thumbnail_url: "Dana Whitfield" },
                    domain: "Dana Whitfield",
                    occurred_at: "2026-01-01T12:00:00Z",
                    platform: "Dana Whitfield",
                    type: "ad_click",
                    utm_source: "Dana Whitfield",
                },
            ],
            timezone: "Dana Whitfield",
            usage: {
                browser: [{ events: 10, value: "Chrome" }],
                city: [{ events: 9, value: "Austin" }],
                country: [{ events: 12, value: "US" }],
                device: [{ events: 5, value: "iPhone" }],
                ip: [{ events: 3, value: "1.1.1.1" }],
                os: [{ events: 7, value: "Mac OS X" }],
                referrer: [{ events: 4, value: "google.com" }],
                timezone: [{ events: 12, value: "America/Chicago" }],
            },
            user: {
                id: "user_dana1",
                name: "Dana Whitfield",
                profile_pic_url: "Dana Whitfield",
                username: "danawhitfield",
            },
            user_ids: ["Dana Whitfield"],
        };

        server.mockEndpoint().get("/people/id").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.people.retrieve({
            id: "id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("retrieve (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/people/id").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.people.retrieve({
                id: "id",
            });
        }).rejects.toThrow(Whop.UnauthorizedError);
    });

    test("retrieve (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/people/id").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.people.retrieve({
                id: "id",
            });
        }).rejects.toThrow(Whop.NotFoundError);
    });
});
