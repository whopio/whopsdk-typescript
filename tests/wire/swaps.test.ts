// This file was auto-generated by Fern from our API Definition.

import * as Whop from "../../src/api/index";
import { WhopClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("SwapsClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: [
                {
                    account_id: "biz_xxxxxxxxxxxxxx",
                    error: "Swap reverted: quoted price moved beyond the slippage tolerance",
                    id: "vault-swap-cwal_xxxxxxxxxxxxxx",
                    object: "swap",
                    status: "queued",
                    tx_hashes: ["0xabc"],
                },
            ],
        };

        server.mockEndpoint().get("/swaps").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.swaps.list({
            account_id: "account_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/swaps").respondWith().statusCode(403).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.swaps.list({
                account_id: "account_id",
            });
        }).rejects.toThrow(Whop.ForbiddenError);
    });

    test("create (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { account_id: "biz_xxxxxxxxxxxxxx", from_token: "usd", to_token: "cad" };
        const rawResponseBody = {
            account_id: "biz_xxxxxxxxxxxxxx",
            amount_in: 131.4,
            amount_out: 180,
            amount_out_expected: "98.9",
            amount_out_min: "98.4",
            from_token: { symbol: "usd" },
            id: "vault-swap-cwal_xxxxxxxxxxxxxx",
            object: "swap",
            rate: "0.989",
            status: "queued",
            to_chain: "ethereum",
            to_token: { symbol: "cad" },
        };

        server
            .mockEndpoint()
            .post("/swaps")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.swaps.create({
            account_id: "biz_xxxxxxxxxxxxxx",
            from_token: "usd",
            to_token: "cad",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("create (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { account_id: "account_id", from_token: "from_token", to_token: "to_token" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/swaps")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.swaps.create({
                account_id: "account_id",
                from_token: "from_token",
                to_token: "to_token",
            });
        }).rejects.toThrow(Whop.BadRequestError);
    });

    test("create (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { account_id: "account_id", from_token: "from_token", to_token: "to_token" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/swaps")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.swaps.create({
                account_id: "account_id",
                from_token: "from_token",
                to_token: "to_token",
            });
        }).rejects.toThrow(Whop.ForbiddenError);
    });

    test("create (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { account_id: "account_id", from_token: "from_token", to_token: "to_token" };
        const rawResponseBody = { error: { message: "message", type: "type" } };

        server
            .mockEndpoint()
            .post("/swaps")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.swaps.create({
                account_id: "account_id",
                from_token: "from_token",
                to_token: "to_token",
            });
        }).rejects.toThrow(Whop.ConflictError);
    });

    test("createQuote (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = {
            amount: "100",
            from_token: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
            to_token: "0x1b64b9025eebb9a6239575df9ea4b9ac46d4d193",
        };
        const rawResponseBody = {
            amount_in: "100",
            amount_out: "0.15",
            amount_out_min: "0.148",
            bridge_fee: "0.25",
            estimated_duration_seconds: 30,
            fee_bps: 100,
            from_address: "0x6f1c0a3d5b2e4f8a9c7d1e0b3a5f7c9d2e4b6a80",
            from_token: {
                address: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
                chain_id: 9745,
                decimals: 6,
                network: "plasma",
                symbol: "USDT",
            },
            metadata: { external_id: "shop_4417" },
            object: "swap_quote",
            rate: "1.5",
            requires_token_approval: true,
            to_address: "0x9d2e4b6a806f1c0a3d5b2e4f8a9c7d1e0b3a5f7c",
            to_token: {
                address: "0x1b64b9025eebb9a6239575df9ea4b9ac46d4d193",
                chain_id: 9745,
                decimals: 6,
                network: "plasma",
                symbol: "XAUT",
            },
        };

        server
            .mockEndpoint()
            .post("/swaps/quote")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.swaps.createQuote({
            amount: "100",
            from_token: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
            to_token: "0x1b64b9025eebb9a6239575df9ea4b9ac46d4d193",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("createQuote (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { amount: "amount", from_token: "from_token", to_token: "to_token" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/swaps/quote")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.swaps.createQuote({
                amount: "amount",
                from_token: "from_token",
                to_token: "to_token",
            });
        }).rejects.toThrow(Whop.BadRequestError);
    });

    test("createQuote (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { amount: "amount", from_token: "from_token", to_token: "to_token" };
        const rawResponseBody = { error: { message: "message", type: "type" } };

        server
            .mockEndpoint()
            .post("/swaps/quote")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.swaps.createQuote({
                amount: "amount",
                from_token: "from_token",
                to_token: "to_token",
            });
        }).rejects.toThrow(Whop.ConflictError);
    });

    test("retrieve (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            account_id: "biz_xxxxxxxxxxxxxx",
            error: "Swap reverted: quoted price moved beyond the slippage tolerance",
            id: "vault-swap-cwal_xxxxxxxxxxxxxx",
            object: "swap",
            status: "queued",
            tx_hashes: ["0xabc"],
        };

        server.mockEndpoint().get("/swaps/id").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.swaps.retrieve({
            id: "id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("retrieve (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/swaps/id").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.swaps.retrieve({
                id: "id",
            });
        }).rejects.toThrow(Whop.BadRequestError);
    });

    test("retrieve (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/swaps/id").respondWith().statusCode(403).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.swaps.retrieve({
                id: "id",
            });
        }).rejects.toThrow(Whop.ForbiddenError);
    });

    test("retrieve (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/swaps/id").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.swaps.retrieve({
                id: "id",
            });
        }).rejects.toThrow(Whop.NotFoundError);
    });
});
