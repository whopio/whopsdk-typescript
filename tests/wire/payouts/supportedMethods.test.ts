// This file was auto-generated by Fern from our API Definition.

import * as Whop from "../../../src/api/index";
import { WhopClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("SupportedMethodsClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: [
                {
                    delivery_type: "cash_pickup",
                    icon_url: "https://whop-assets-example.s3.amazonaws.com/uploads/image/2026-01-01/ach-bank-deposit",
                    id: "podst_xxxxxxxxxxxxxx",
                    name: "SEPA Bank Transfer",
                    object: "supported_payout_method",
                    quotes: [
                        {
                            amount: 2500,
                            currency: "usd",
                            destination_currency: "usd",
                            exchange_rate: 1,
                            instant: { estimated_arrival: "2026-01-01T12:00:00Z", fee: 12.5, total_received: 2487.5 },
                            max_limit: 50000,
                            min_limit: 10,
                            standard: { estimated_arrival: "2026-01-01T12:00:00Z", fee: 2.5, total_received: 2497.5 },
                        },
                    ],
                    required_fields: [
                        {
                            id: "attr_account_number",
                            input_type: "text",
                            label: "Account Number",
                            object: "required_field",
                            options: ["savings"],
                            placeholder: "8825310472",
                            required: true,
                            sensitive: true,
                            type: "bank_account_number",
                            validation: "^[0-9]{4,17}$",
                        },
                    ],
                    supports_instant_delivery: false,
                    supports_standard_delivery: true,
                },
            ],
            page_info: { end_cursor: "MQ", has_next_page: true, has_previous_page: false, start_cursor: "MQ" },
        };

        server
            .mockEndpoint({ once: false })
            .get("/payouts/supported_methods")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = rawResponseBody;
        const page = await client.payouts.supportedMethods.list();

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/payouts/supported_methods")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payouts.supportedMethods.list();
        }).rejects.toThrow(Whop.BadRequestError);
    });

    test("list (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/payouts/supported_methods")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payouts.supportedMethods.list();
        }).rejects.toThrow(Whop.UnauthorizedError);
    });

    test("list (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/payouts/supported_methods")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payouts.supportedMethods.list();
        }).rejects.toThrow(Whop.ForbiddenError);
    });

    test("list (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new WhopClient({
            maxRetries: 0,
            token: "test",
            apiVersionDate: "test",
            idempotencyKey: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/payouts/supported_methods")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payouts.supportedMethods.list();
        }).rejects.toThrow(Whop.NotFoundError);
    });
});
