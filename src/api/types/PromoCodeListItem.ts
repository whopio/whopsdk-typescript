// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

export interface PromoCodeListItem {
    /** Discount amount. Percentage discounts are represented as a decimal fraction. */
    amount_off: number;
    /** Whether the promo code is restricted to churned customers. */
    churned_users_only: boolean;
    /** Code entered at checkout. */
    code: string | null;
    /** When the promo code was created, as an ISO 8601 timestamp. */
    created_at: string;
    /** Currency used for a fixed-amount discount. */
    currency: PromoCodeListItem.Currency;
    /** How long the discount applies. */
    duration: PromoCodeListItem.Duration;
    /** Whether the promo code applies only to existing memberships. */
    existing_memberships_only: boolean;
    /** When the promo code expires, as an ISO 8601 timestamp. */
    expires_at: string | null;
    /** Promo code ID, prefixed `promo_`. */
    id: string;
    /** Custom key-value metadata stored on the promo code. */
    metadata: Record<string, unknown>;
    /** Whether the promo code is restricted to new customers. */
    new_users_only: boolean;
    /** Whether each customer may redeem the promo code only once. */
    one_per_customer: boolean;
    /** Product the promo code is restricted to, or `null` when it is not product-scoped. */
    product: Whop.PromoCodeProduct | null;
    /** Billing intervals the discount applies to. */
    promo_duration_months: number | null;
    /** Whether the discount is percentage-based or a fixed amount. */
    promo_type: PromoCodeListItem.PromoType;
    /** Promo code lifecycle status. */
    status: PromoCodeListItem.Status;
    /** Maximum uses when stock is limited. */
    stock: number;
    /** Whether the promo code has no redemption limit. */
    unlimited_stock: boolean;
    /** When the promo code was updated, as an ISO 8601 timestamp. */
    updated_at: string;
    /** Memberships that used the promo code. */
    uses: number;
}

export namespace PromoCodeListItem {
    /** Currency used for a fixed-amount discount. */
    export const Currency = {
        Usd: "usd",
        Sgd: "sgd",
        Inr: "inr",
        Aud: "aud",
        Brl: "brl",
        Cad: "cad",
        Dkk: "dkk",
        Eur: "eur",
        Nok: "nok",
        Gbp: "gbp",
        Sek: "sek",
        Chf: "chf",
        Hkd: "hkd",
        Huf: "huf",
        Jpy: "jpy",
        Mxn: "mxn",
        Myr: "myr",
        Pln: "pln",
        Czk: "czk",
        Nzd: "nzd",
        Aed: "aed",
        Eth: "eth",
        Ape: "ape",
        Cop: "cop",
        Ron: "ron",
        Thb: "thb",
        Bgn: "bgn",
        Idr: "idr",
        Dop: "dop",
        Php: "php",
        Try: "try",
        Krw: "krw",
        Twd: "twd",
        Vnd: "vnd",
        Pkr: "pkr",
        Clp: "clp",
        Uyu: "uyu",
        Ars: "ars",
        Zar: "zar",
        Dzd: "dzd",
        Tnd: "tnd",
        Mad: "mad",
        Kes: "kes",
        Kwd: "kwd",
        Jod: "jod",
        All: "all",
        Xcd: "xcd",
        Amd: "amd",
        Bsd: "bsd",
        Bhd: "bhd",
        Bob: "bob",
        Bam: "bam",
        Khr: "khr",
        Crc: "crc",
        Xof: "xof",
        Egp: "egp",
        Etb: "etb",
        Gmd: "gmd",
        Ghs: "ghs",
        Gtq: "gtq",
        Gyd: "gyd",
        Ils: "ils",
        Jmd: "jmd",
        Mop: "mop",
        Mga: "mga",
        Mur: "mur",
        Mdl: "mdl",
        Mnt: "mnt",
        Nad: "nad",
        Ngn: "ngn",
        Mkd: "mkd",
        Omr: "omr",
        Pyg: "pyg",
        Pen: "pen",
        Qar: "qar",
        Rwf: "rwf",
        Sar: "sar",
        Rsd: "rsd",
        Lkr: "lkr",
        Tzs: "tzs",
        Ttd: "ttd",
        Uzs: "uzs",
        Rub: "rub",
        Btc: "btc",
        Cny: "cny",
        Usdt: "usdt",
        Kzt: "kzt",
        Awg: "awg",
        WhopUsd: "whop_usd",
        Xau: "xau",
    } as const;
    export type Currency = (typeof Currency)[keyof typeof Currency];
    /** How long the discount applies. */
    export const Duration = {
        Forever: "forever",
        Once: "once",
        Repeating: "repeating",
    } as const;
    export type Duration = (typeof Duration)[keyof typeof Duration];
    /** Whether the discount is percentage-based or a fixed amount. */
    export const PromoType = {
        Percentage: "percentage",
        FlatAmount: "flat_amount",
    } as const;
    export type PromoType = (typeof PromoType)[keyof typeof PromoType];
    /** Promo code lifecycle status. */
    export const Status = {
        Active: "active",
        Inactive: "inactive",
        Archived: "archived",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
