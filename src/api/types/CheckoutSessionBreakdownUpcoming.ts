// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

/**
 * One amount owed after today. `type` picks the shape — `one_time` is a single later charge, `recurring` an open-ended subscription, `installments` a fixed count of payments — and each variant carries only its own fields, so switching on `type` gives you exactly that entry's payload.
 */
export type CheckoutSessionBreakdownUpcoming =
    | Whop.CheckoutSessionBreakdownUpcoming.Installments
    | Whop.CheckoutSessionBreakdownUpcoming.OneTime
    | Whop.CheckoutSessionBreakdownUpcoming.Recurring;

export namespace CheckoutSessionBreakdownUpcoming {
    export interface Installments extends Whop.CheckoutSessionBreakdownUpcomingInstallments {
        type: "installments";
    }

    export interface OneTime extends Whop.CheckoutSessionBreakdownUpcomingOneTime {
        type: "one_time";
    }

    export interface Recurring extends Whop.CheckoutSessionBreakdownUpcomingRecurring {
        type: "recurring";
    }
}
