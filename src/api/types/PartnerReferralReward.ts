// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

export interface PartnerReferralReward {
    /** Activity that qualifies for this reward, when specified. */
    bot_qualification_type: PartnerReferralReward.BotQualificationType | null;
    /** Partner reward ID, prefixed `prwd_`. */
    id: string;
    /** USD amount of qualifying activity required to earn the reward. */
    qualification_amount: Whop.Money;
    /** Reward recipient's role, or null for a fixed user who is not the requesting partner. */
    recipient: PartnerReferralReward.Recipient | null;
    /** Concrete recipient's user or business ID, or null until a business redeems the offer. */
    recipient_id: string | null;
    /** USD amount granted after qualification. */
    reward_amount: Whop.Money;
}

export namespace PartnerReferralReward {
    /** Activity that qualifies for this reward, when specified. */
    export const BotQualificationType = {
        Sales: "sales",
        AdSpend: "ad_spend",
    } as const;
    export type BotQualificationType = (typeof BotQualificationType)[keyof typeof BotQualificationType];
    /** Reward recipient's role, or null for a fixed user who is not the requesting partner. */
    export const Recipient = {
        Business: "business",
        Partner: "partner",
    } as const;
    export type Recipient = (typeof Recipient)[keyof typeof Recipient];
}
