// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

export type PaymentMethodListItem =
    /**
     * A saved payment method with no type-specific details available. */
    | Whop.PaymentMethodListItem.BasePaymentMethod
    /**
     * A saved card payment method, including brand, last four digits, and expiration details. */
    | Whop.PaymentMethodListItem.CardPaymentMethod
    /**
     * A saved US bank account payment method, including bank name, last four digits, and account type. */
    | Whop.PaymentMethodListItem.UsBankAccountPaymentMethod
    /**
     * A saved Cash App payment method, including the buyer's cashtag and unique identifier. */
    | Whop.PaymentMethodListItem.CashappPaymentMethod
    /**
     * A saved iDEAL payment method, including the customer's bank name and BIC code. */
    | Whop.PaymentMethodListItem.IdealPaymentMethod
    /**
     * A saved SEPA Direct Debit payment method, including the bank code, country, and last four IBAN digits. */
    | Whop.PaymentMethodListItem.SepaDebitPaymentMethod
    /**
     * A Whop balance the buyer can pay with — their own, or an account's they hold permission to spend. Charged by naming its ledger id on a `saved` confirmation token — it is a live wallet, not a stored credential, so it cannot be vaulted or charged off-session. */
    | Whop.PaymentMethodListItem.PlatformBalancePaymentMethod;

export namespace PaymentMethodListItem {
    export interface BasePaymentMethod {
        typename: "BasePaymentMethod";
        /** The time of the event in ISO 8601 UTC format with millisecond precision */
        created_at: string;
        /** Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art. */
        icons: PaymentMethodListItemBasePaymentMethod.Icons;
        /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
        id: string;
        /** The type of payment instrument stored on file (e.g., card, us_bank_account, cashapp, ideal, sepa_debit). */
        payment_method_type: Whop.PaymentMethodTypes;
    }

    export namespace PaymentMethodListItemBasePaymentMethod {
        /**
         * Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art.
         */
        export interface Icons {
            /** The credit-card-proportioned tile (48x30). */
            card: Icons.Card;
            /** The square tile (32x32). */
            square: Icons.Square;
        }

        export namespace Icons {
            /**
             * The credit-card-proportioned tile (48x30).
             */
            export interface Card {
                /** The colorway for dark surfaces. */
                dark: Card.Dark;
                /** The colorway for light surfaces. */
                light: Card.Light;
            }

            export namespace Card {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }

            /**
             * The square tile (32x32).
             */
            export interface Square {
                /** The colorway for dark surfaces. */
                dark: Square.Dark;
                /** The colorway for light surfaces. */
                light: Square.Light;
            }

            export namespace Square {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }
        }
    }

    export interface CardPaymentMethod {
        typename: "CardPaymentMethod";
        /** The card-specific details for this payment method, including brand, last four digits, and expiration. */
        card: PaymentMethodListItemCardPaymentMethod.Card;
        /** The time of the event in ISO 8601 UTC format with millisecond precision */
        created_at: string;
        /** Whether this card has the payer identity document required by its payment provider. */
        has_payer_document: boolean;
        /** Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art. */
        icons: PaymentMethodListItemCardPaymentMethod.Icons;
        /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
        id: string;
        /** The type of payment instrument stored on file (e.g., card, us_bank_account, cashapp, ideal, sepa_debit). */
        payment_method_type: Whop.PaymentMethodTypes;
    }

    export namespace PaymentMethodListItemCardPaymentMethod {
        /**
         * The card-specific details for this payment method, including brand, last four digits, and expiration.
         */
        export interface Card {
            /** The card network (e.g., visa, mastercard, amex). Null if the brand could not be determined. */
            brand: Whop.CardBrands | null;
            /** The two-digit expiration month of the card (1-12). Null if not available. */
            exp_month: number | null;
            /** The two-digit expiration year of the card (e.g., 27 for 2027). Null if not available. */
            exp_year: number | null;
            /** Whether the card is past its expiration month. An expired card cannot take a new charge. */
            expired: boolean;
            /** A stable identifier for the underlying card. Two payment methods with the same fingerprint are the same card. Null if not available. */
            fingerprint: string | null;
            /** How the card is funded by the issuer. Null if the funding type could not be determined. */
            funding_type: Whop.CardFundingTypes | null;
            /** The last four digits of the card number. Null if not available. */
            last4: string | null;
            /** Whether this card was verified with 3D Secure, either when it was saved or on a payment that used it. */
            three_ds_verified: boolean;
        }

        /**
         * Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art.
         */
        export interface Icons {
            /** The credit-card-proportioned tile (48x30). */
            card: Icons.Card;
            /** The square tile (32x32). */
            square: Icons.Square;
        }

        export namespace Icons {
            /**
             * The credit-card-proportioned tile (48x30).
             */
            export interface Card {
                /** The colorway for dark surfaces. */
                dark: Card.Dark;
                /** The colorway for light surfaces. */
                light: Card.Light;
            }

            export namespace Card {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }

            /**
             * The square tile (32x32).
             */
            export interface Square {
                /** The colorway for dark surfaces. */
                dark: Square.Dark;
                /** The colorway for light surfaces. */
                light: Square.Light;
            }

            export namespace Square {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }
        }
    }

    export interface UsBankAccountPaymentMethod {
        typename: "UsBankAccountPaymentMethod";
        /** The time of the event in ISO 8601 UTC format with millisecond precision */
        created_at: string;
        /** Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art. */
        icons: PaymentMethodListItemUsBankAccountPaymentMethod.Icons;
        /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
        id: string;
        /** The type of payment instrument stored on file (e.g., card, us_bank_account, cashapp, ideal, sepa_debit). */
        payment_method_type: Whop.PaymentMethodTypes;
        /** The bank account-specific details for this payment method, including bank name and last four digits. */
        us_bank_account: PaymentMethodListItemUsBankAccountPaymentMethod.UsBankAccount;
    }

    export namespace PaymentMethodListItemUsBankAccountPaymentMethod {
        /**
         * Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art.
         */
        export interface Icons {
            /** The credit-card-proportioned tile (48x30). */
            card: Icons.Card;
            /** The square tile (32x32). */
            square: Icons.Square;
        }

        export namespace Icons {
            /**
             * The credit-card-proportioned tile (48x30).
             */
            export interface Card {
                /** The colorway for dark surfaces. */
                dark: Card.Dark;
                /** The colorway for light surfaces. */
                light: Card.Light;
            }

            export namespace Card {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }

            /**
             * The square tile (32x32).
             */
            export interface Square {
                /** The colorway for dark surfaces. */
                dark: Square.Dark;
                /** The colorway for light surfaces. */
                light: Square.Light;
            }

            export namespace Square {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }
        }

        /**
         * The bank account-specific details for this payment method, including bank name and last four digits.
         */
        export interface UsBankAccount {
            /** The type of bank account (e.g., checking, savings). */
            account_type: string;
            /** The name of the financial institution holding the account. */
            bank_name: string;
            /** The last four digits of the bank account number. */
            last4: string;
        }
    }

    export interface CashappPaymentMethod {
        typename: "CashappPaymentMethod";
        /** The Cash App-specific details for this payment method, including cashtag and buyer ID. */
        cashapp: PaymentMethodListItemCashappPaymentMethod.Cashapp;
        /** The time of the event in ISO 8601 UTC format with millisecond precision */
        created_at: string;
        /** Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art. */
        icons: PaymentMethodListItemCashappPaymentMethod.Icons;
        /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
        id: string;
        /** The type of payment instrument stored on file (e.g., card, us_bank_account, cashapp, ideal, sepa_debit). */
        payment_method_type: Whop.PaymentMethodTypes;
    }

    export namespace PaymentMethodListItemCashappPaymentMethod {
        /**
         * The Cash App-specific details for this payment method, including cashtag and buyer ID.
         */
        export interface Cashapp {
            /** The unique and immutable identifier assigned by Cash App to the buyer. Null if not available. */
            buyer_id: string | null;
            /** The public cashtag handle of the buyer on Cash App. Null if not available. */
            cashtag: string | null;
        }

        /**
         * Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art.
         */
        export interface Icons {
            /** The credit-card-proportioned tile (48x30). */
            card: Icons.Card;
            /** The square tile (32x32). */
            square: Icons.Square;
        }

        export namespace Icons {
            /**
             * The credit-card-proportioned tile (48x30).
             */
            export interface Card {
                /** The colorway for dark surfaces. */
                dark: Card.Dark;
                /** The colorway for light surfaces. */
                light: Card.Light;
            }

            export namespace Card {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }

            /**
             * The square tile (32x32).
             */
            export interface Square {
                /** The colorway for dark surfaces. */
                dark: Square.Dark;
                /** The colorway for light surfaces. */
                light: Square.Light;
            }

            export namespace Square {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }
        }
    }

    export interface IdealPaymentMethod {
        typename: "IdealPaymentMethod";
        /** The time of the event in ISO 8601 UTC format with millisecond precision */
        created_at: string;
        /** Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art. */
        icons: PaymentMethodListItemIdealPaymentMethod.Icons;
        /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
        id: string;
        /** The iDEAL-specific details for this payment method, including bank name and BIC. */
        ideal: PaymentMethodListItemIdealPaymentMethod.Ideal;
        /** The type of payment instrument stored on file (e.g., card, us_bank_account, cashapp, ideal, sepa_debit). */
        payment_method_type: Whop.PaymentMethodTypes;
    }

    export namespace PaymentMethodListItemIdealPaymentMethod {
        /**
         * Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art.
         */
        export interface Icons {
            /** The credit-card-proportioned tile (48x30). */
            card: Icons.Card;
            /** The square tile (32x32). */
            square: Icons.Square;
        }

        export namespace Icons {
            /**
             * The credit-card-proportioned tile (48x30).
             */
            export interface Card {
                /** The colorway for dark surfaces. */
                dark: Card.Dark;
                /** The colorway for light surfaces. */
                light: Card.Light;
            }

            export namespace Card {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }

            /**
             * The square tile (32x32).
             */
            export interface Square {
                /** The colorway for dark surfaces. */
                dark: Square.Dark;
                /** The colorway for light surfaces. */
                light: Square.Light;
            }

            export namespace Square {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }
        }

        /**
         * The iDEAL-specific details for this payment method, including bank name and BIC.
         */
        export interface Ideal {
            /** The name of the customer's bank used for the iDEAL transaction. Null if not available. */
            bank: string | null;
            /** The Bank Identifier Code (BIC/SWIFT) of the customer's bank. Null if not available. */
            bic: string | null;
        }
    }

    export interface SepaDebitPaymentMethod {
        typename: "SepaDebitPaymentMethod";
        /** The time of the event in ISO 8601 UTC format with millisecond precision */
        created_at: string;
        /** Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art. */
        icons: PaymentMethodListItemSepaDebitPaymentMethod.Icons;
        /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
        id: string;
        /** The type of payment instrument stored on file (e.g., card, us_bank_account, cashapp, ideal, sepa_debit). */
        payment_method_type: Whop.PaymentMethodTypes;
        /** The SEPA Direct Debit-specific details for this payment method, including bank code and last four IBAN digits. */
        sepa_debit: PaymentMethodListItemSepaDebitPaymentMethod.SepaDebit;
    }

    export namespace PaymentMethodListItemSepaDebitPaymentMethod {
        /**
         * Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art.
         */
        export interface Icons {
            /** The credit-card-proportioned tile (48x30). */
            card: Icons.Card;
            /** The square tile (32x32). */
            square: Icons.Square;
        }

        export namespace Icons {
            /**
             * The credit-card-proportioned tile (48x30).
             */
            export interface Card {
                /** The colorway for dark surfaces. */
                dark: Card.Dark;
                /** The colorway for light surfaces. */
                light: Card.Light;
            }

            export namespace Card {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }

            /**
             * The square tile (32x32).
             */
            export interface Square {
                /** The colorway for dark surfaces. */
                dark: Square.Dark;
                /** The colorway for light surfaces. */
                light: Square.Light;
            }

            export namespace Square {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }
        }

        /**
         * The SEPA Direct Debit-specific details for this payment method, including bank code and last four IBAN digits.
         */
        export interface SepaDebit {
            /** The bank code of the financial institution associated with this SEPA account. Null if not available. */
            bank_code: string | null;
            /** The branch code of the financial institution associated with this SEPA account. Null if not available. */
            branch_code: string | null;
            /** The two-letter ISO country code where the bank account is located. Null if not available. */
            country: string | null;
            /** The last four digits of the IBAN associated with this SEPA account. Null if not available. */
            last4: string | null;
        }
    }

    export interface PlatformBalancePaymentMethod {
        typename: "PlatformBalancePaymentMethod";
        /** The time of the event in ISO 8601 UTC format with millisecond precision */
        created_at: string;
        /** Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art. */
        icons: PaymentMethodListItemPlatformBalancePaymentMethod.Icons;
        /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
        id: string;
        /** The type of payment instrument stored on file (e.g., card, us_bank_account, cashapp, ideal, sepa_debit). */
        payment_method_type: Whop.PaymentMethodTypes;
        /** What is available to spend, and whether the account may spend it. */
        platform_balance: PaymentMethodListItemPlatformBalancePaymentMethod.PlatformBalance;
    }

    export namespace PaymentMethodListItemPlatformBalancePaymentMethod {
        /**
         * Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art.
         */
        export interface Icons {
            /** The credit-card-proportioned tile (48x30). */
            card: Icons.Card;
            /** The square tile (32x32). */
            square: Icons.Square;
        }

        export namespace Icons {
            /**
             * The credit-card-proportioned tile (48x30).
             */
            export interface Card {
                /** The colorway for dark surfaces. */
                dark: Card.Dark;
                /** The colorway for light surfaces. */
                light: Card.Light;
            }

            export namespace Card {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }

            /**
             * The square tile (32x32).
             */
            export interface Square {
                /** The colorway for dark surfaces. */
                dark: Square.Dark;
                /** The colorway for light surfaces. */
                light: Square.Light;
            }

            export namespace Square {
                /**
                 * The colorway for dark surfaces.
                 */
                export interface Dark {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }

                /**
                 * The colorway for light surfaces.
                 */
                export interface Light {
                    /** Raster fallback at the shape's native size. */
                    png_1x: string;
                    /** Raster fallback at double density. */
                    png_2x: string;
                    /** Raster fallback at quadruple density. */
                    png_4x: string;
                    /** The vector file. Prefer this everywhere SVG renders. */
                    svg: string;
                }
            }
        }

        /**
         * What is available to spend, and whether the account may spend it.
         */
        export interface PlatformBalance {
            /** The account whose wallet this is. Null for the buyer's own personal wallet. A buyer sees an account's balance here when they hold permission to spend it, so a list can hold several — their own and one per account they are on. */
            account: PlatformBalance.Account | null;
            /** Available amount per currency. Read from the balance cache, so it is indicative — the charge revalidates against settled funds and may still refuse. */
            balances: PlatformBalance.Balances.Item[];
            /** Whether this balance can pay right now, which here means only whether it holds funds — an account blocked from spending is not listed at all. A zero balance is still returned so a client can show it as an option the buyer could top up. */
            spendable: boolean;
        }

        export namespace PlatformBalance {
            /**
             * The account whose wallet this is. Null for the buyer's own personal wallet. A buyer sees an account's balance here when they hold permission to spend it, so a list can hold several — their own and one per account they are on.
             */
            export interface Account {
                /** The unique identifier for the company. */
                id: string;
                /** The company's logo. */
                logo: Account.Logo | null;
                /** The display name of the company shown to customers. */
                title: string;
            }

            export namespace Account {
                /**
                 * The company's logo.
                 */
                export interface Logo {
                    /** A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps. */
                    url: string | null;
                }
            }

            export type Balances = Balances.Item[];

            export namespace Balances {
                /**
                 * An amount of money. Never a bare number, because a bare number cannot answer the two questions a client has to answer to render it: what currency is this, and how many digits do I write? The second is stated twice rather than derived, because the digits the amount CARRIES and the digits to SHOW differ in COP — charged in centavos, written in whole pesos. Formatting is deliberately left to the caller: the number belongs in the buyer's locale, and this API does not know it.
                 */
                export interface Item {
                    /** The amount in major units, as an exact decimal string — `"10.00"` is ten dollars. A string so no float rounds it in transit. */
                    amount: string;
                    /** Three-letter ISO 4217 currency code, lowercase. */
                    currency: Whop.Currencies;
                    /** How many decimal places the amount CARRIES — the precision the charge itself runs at. */
                    decimals: number;
                    /** How many decimal places to SHOW. Usually equal to `decimals`, and deliberately not always: COP is charged in centavos but written in whole pesos, so it is `2` and `0`. Format the number in your own locale using this. */
                    display_decimals: number;
                }
            }
        }
    }
}
