// This file was auto-generated by Fern from our API Definition.

/**
 * Device platforms and operating systems to target.
 */
export interface AdGroupDevicesBody {
    /** Operating systems to target. Empty targets all operating systems. */
    operating_systems?: AdGroupDevicesBody.OperatingSystems.Item[] | undefined;
    /** Device types to target. Empty targets all devices. */
    platforms?: AdGroupDevicesBody.Platforms.Item[] | undefined;
}

export namespace AdGroupDevicesBody {
    export type OperatingSystems = OperatingSystems.Item[];

    export namespace OperatingSystems {
        export interface Item {
            /** Lowest OS version to target, such as `18.0`. Omit to target any version. */
            minimum_version?: string | undefined;
            /** Operating system to target. */
            os: Item.Os;
        }

        export namespace Item {
            /** Operating system to target. */
            export const Os = {
                Ios: "ios",
                Android: "android",
            } as const;
            export type Os = (typeof Os)[keyof typeof Os];
        }
    }

    export type Platforms = Platforms.Item[];

    export namespace Platforms {
        export const Item = {
            Mobile: "mobile",
            Desktop: "desktop",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }
}
