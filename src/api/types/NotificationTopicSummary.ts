// This file was auto-generated by Fern from our API Definition.

export interface NotificationTopicSummary {
    /** Whether notifications for this topic are enabled by default when the user has not set a preference. */
    default_preference_value: boolean;
    /** Notification topic ID, prefixed `topic_`. */
    id: string;
    /** Whether this topic exclusively handles mention-based notifications. */
    is_mention: boolean;
    /** Scope of the topic: whether it applies to an account, a user, or an account's team. */
    topic_type: NotificationTopicSummary.TopicType;
}

export namespace NotificationTopicSummary {
    /** Scope of the topic: whether it applies to an account, a user, or an account's team. */
    export const TopicType = {
        Account: "account",
        User: "user",
        AccountTeam: "account_team",
    } as const;
    export type TopicType = (typeof TopicType)[keyof typeof TopicType];
}
