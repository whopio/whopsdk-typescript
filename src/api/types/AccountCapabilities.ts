// This file was auto-generated by Fern from our API Definition.

export interface AccountCapabilities {
    /** Bank payins: debits, transfers, and local bank rails */
    accept_bank_payments: AccountCapabilities.AcceptBankPayments;
    /** Buy-now-pay-later payins; requires approval */
    accept_bnpl_payments: AccountCapabilities.AcceptBnplPayments;
    /** Card payins, including Apple Pay and Google Pay */
    accept_card_payments: AccountCapabilities.AcceptCardPayments;
    /** Deposits by bank wire or ACH to the account's virtual bank account */
    bank_deposit: AccountCapabilities.BankDeposit;
    /** Balance top-ups by charging a stored payment method */
    card_deposit: AccountCapabilities.CardDeposit;
    /** Issuing Whop cards; requires card application approval */
    card_issuing: AccountCapabilities.CardIssuing;
    /** On-chain deposits to the account's crypto wallet */
    crypto_deposit: AccountCapabilities.CryptoDeposit;
    /** On-chain payouts to a crypto wallet */
    crypto_payout: AccountCapabilities.CryptoPayout;
    /** Instant payouts to an eligible payout destination */
    instant_payout: AccountCapabilities.InstantPayout;
    /** Launching ad campaigns through Whop Ads. `inactive` while a requested ads services agreement is awaiting the account's signature. */
    run_ads: AccountCapabilities.RunAds;
    /** Standard payouts to an external payout destination */
    standard_payout: AccountCapabilities.StandardPayout;
    /** Transfers to other accounts */
    transfer: AccountCapabilities.Transfer;
}

export namespace AccountCapabilities {
    /** Bank payins: debits, transfers, and local bank rails */
    export const AcceptBankPayments = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type AcceptBankPayments = (typeof AcceptBankPayments)[keyof typeof AcceptBankPayments];
    /** Buy-now-pay-later payins; requires approval */
    export const AcceptBnplPayments = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type AcceptBnplPayments = (typeof AcceptBnplPayments)[keyof typeof AcceptBnplPayments];
    /** Card payins, including Apple Pay and Google Pay */
    export const AcceptCardPayments = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type AcceptCardPayments = (typeof AcceptCardPayments)[keyof typeof AcceptCardPayments];
    /** Deposits by bank wire or ACH to the account's virtual bank account */
    export const BankDeposit = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type BankDeposit = (typeof BankDeposit)[keyof typeof BankDeposit];
    /** Balance top-ups by charging a stored payment method */
    export const CardDeposit = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type CardDeposit = (typeof CardDeposit)[keyof typeof CardDeposit];
    /** Issuing Whop cards; requires card application approval */
    export const CardIssuing = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type CardIssuing = (typeof CardIssuing)[keyof typeof CardIssuing];
    /** On-chain deposits to the account's crypto wallet */
    export const CryptoDeposit = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type CryptoDeposit = (typeof CryptoDeposit)[keyof typeof CryptoDeposit];
    /** On-chain payouts to a crypto wallet */
    export const CryptoPayout = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type CryptoPayout = (typeof CryptoPayout)[keyof typeof CryptoPayout];
    /** Instant payouts to an eligible payout destination */
    export const InstantPayout = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type InstantPayout = (typeof InstantPayout)[keyof typeof InstantPayout];
    /** Launching ad campaigns through Whop Ads. `inactive` while a requested ads services agreement is awaiting the account's signature. */
    export const RunAds = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type RunAds = (typeof RunAds)[keyof typeof RunAds];
    /** Standard payouts to an external payout destination */
    export const StandardPayout = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type StandardPayout = (typeof StandardPayout)[keyof typeof StandardPayout];
    /** Transfers to other accounts */
    export const Transfer = {
        Active: "active",
        Inactive: "inactive",
        Pending: "pending",
    } as const;
    export type Transfer = (typeof Transfer)[keyof typeof Transfer];
}
