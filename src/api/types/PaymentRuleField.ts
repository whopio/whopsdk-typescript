// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

export interface PaymentRuleField {
    /** The payment attribute this condition reads. */
    field: PaymentRuleField.Field;
    /** The largest value this field accepts, or `null` when the field is not numeric. */
    maximum: number | null;
    /** The smallest value this field accepts, or `null` when the field is not numeric. */
    minimum: number | null;
    operators: PaymentRuleField.Operators.Item[];
    options: Whop.PaymentRuleFieldOption[] | null;
    /** The type of value this field compares against. */
    type: PaymentRuleField.Type;
}

export namespace PaymentRuleField {
    /** The payment attribute this condition reads. */
    export const Field = {
        RiskScore: "risk_score",
        AmountInUsd: "amount_in_usd",
        CardCountry: "card_country",
        CustomerEmail: "customer_email",
        IpAddress: "ip_address",
    } as const;
    export type Field = (typeof Field)[keyof typeof Field];
    export type Operators = Operators.Item[];

    export namespace Operators {
        /** The operators this field accepts. Any other operator is rejected. */
        export const Item = {
            Eq: "eq",
            Neq: "neq",
            Gt: "gt",
            Gte: "gte",
            Lt: "lt",
            Lte: "lte",
            In: "in",
            NotIn: "not_in",
            Contains: "contains",
            StartsWith: "starts_with",
            EndsWith: "ends_with",
            InCidr: "in_cidr",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    /** The type of value this field compares against. */
    export const Type = {
        Integer: "integer",
        Number: "number",
        String: "string",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
}
