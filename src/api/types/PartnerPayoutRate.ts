// This file was auto-generated by Fern from our API Definition.

export interface PartnerPayoutRate {
    /** Income source that generates this percentage payout. */
    income_source: PartnerPayoutRate.IncomeSource;
    /** Partner's default percentage for this tier and income source. For example, 30 means 30%. */
    percentage: number;
}

export namespace PartnerPayoutRate {
    /** Income source that generates this percentage payout. */
    export const IncomeSource = {
        Sales: "sales",
        Transfer: "transfer",
        CardInterchange: "card_interchange",
        AdSpend: "ad_spend",
    } as const;
    export type IncomeSource = (typeof IncomeSource)[keyof typeof IncomeSource];
}
