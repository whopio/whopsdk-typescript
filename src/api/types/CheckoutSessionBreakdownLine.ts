// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

export interface CheckoutSessionBreakdownLine {
    /** The line's own total — `unit_amount` × `quantity`, before any adjustment. */
    amount: Whop.Money;
    /** What to show the buyer for this line. */
    description: string;
    /** What this line is — `plan` today. New kinds arrive as checkout learns to sell more than one thing at once. */
    kind: CheckoutSessionBreakdownLine.Kind;
    /** How many. */
    quantity: number;
    /** Price of one, before any adjustment. */
    unit_amount: Whop.Money;
}

export namespace CheckoutSessionBreakdownLine {
    /** What this line is — `plan` today. New kinds arrive as checkout learns to sell more than one thing at once. */
    export const Kind = {
        Plan: "plan",
    } as const;
    export type Kind = (typeof Kind)[keyof typeof Kind];
}
