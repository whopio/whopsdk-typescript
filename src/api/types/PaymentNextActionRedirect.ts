// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

export interface PaymentNextActionRedirect {
    /** Where to send the buyer. */
    data: Whop.PaymentNextActionRedirectData;
    render: PaymentNextActionRedirect.Render.Item[];
}

export namespace PaymentNextActionRedirect {
    export type Render = Render.Item[];

    export namespace Render {
        /** Where this step can be presented: `inline` inside your own page, `full_page` as a top-level navigation. Pick whichever listed mode suits your surface. */
        export const Item = {
            Inline: "inline",
            FullPage: "full_page",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }
}
