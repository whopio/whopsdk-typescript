// This file was auto-generated by Fern from our API Definition.

/**
 * Description of a node (model) including its columns and associations.
 */
export interface DescribeStatsResponseDescribeNode {
    /** The node path being described. */
    node: string;
    /** The query engine being used. */
    engine: string;
    /** Available columns. */
    columns: string[];
    /** Columns that can be used for sorting. */
    sortable_columns: string[];
    /** Available associations or child paths. */
    associations: DescribeStatsResponseDescribeNode.Associations.Item[];
    /** Available metrics for this node. */
    metrics: DescribeStatsResponseDescribeNode.Metrics.Item[];
    /** Sample data rows. */
    sample: Record<string, unknown>[] | null;
    /** Debug information. */
    debug: DescribeStatsResponseDescribeNode.Debug | null;
}

export namespace DescribeStatsResponseDescribeNode {
    export type Associations = Associations.Item[];

    export namespace Associations {
        export interface Item {
            /** The association name. */
            name: string;
            /** The type (belongs_to, has_many, has_one, event, namespace). */
            type: string;
            /** The associated model class name (for model associations). */
            model: string | null;
            /** The full path (for event associations). */
            path: string | null;
            /** The event name (for event type). */
            event_name: string | null;
        }
    }

    export type Metrics = Metrics.Item[];

    export namespace Metrics {
        export interface Item {
            /** The metric name. */
            name: string;
            /** The node path this metric operates on. */
            node_path: string;
            /** Query engines that support this metric. */
            supported_engines: string[];
        }
    }

    /**
     * Debug information.
     */
    export interface Debug {
        /** Unique request identifier for debugging. */
        request_id: string | null;
    }
}
