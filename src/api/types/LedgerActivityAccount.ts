// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

/**
 * The viewer account that owns this row's ledger. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise.
 */
export type LedgerActivityAccount = Whop.LedgerActivityAccount.Account | Whop.LedgerActivityAccount.User;

export namespace LedgerActivityAccount {
    export interface Account {
        object: "account";
        /** Account ID. */
        id: string;
        /** Account logo URL. */
        logo_url: string | null;
        /** Account route. */
        route: string | null;
        /** Account display name. */
        title: string | null;
    }

    export interface User {
        object: "user";
        /** User ID. */
        id: string;
        /** User display name. */
        name: string | null;
        /** User profile image URL. */
        profile_picture_url: string | null;
        /** User's username. */
        username: string | null;
    }
}
