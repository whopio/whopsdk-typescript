// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

export interface TradingPosition {
    /** Average entry price in USD, or `null` when the provider omits it. */
    entry_price: Whop.Money | null;
    /** Hyperliquid perpetual details. Present on Hyperliquid positions, otherwise `null`. */
    hyperliquid: Whop.TradingHyperliquidPosition | null;
    /** Trading position ID, prefixed `trdpos_`. Stable for a market within one trading account. */
    id: string;
    /** Market symbol on the provider, such as `ETH`. */
    market: string;
    object: TradingPosition.Object_;
    /** Current position value in USD. */
    position_value: Whop.Money;
    side: TradingPosition.Side;
    /** Absolute position size as a decimal string. */
    size: string;
    /** Unrealized profit or loss in USD. Negative for a loss. */
    unrealized_pnl: Whop.Money;
}

export namespace TradingPosition {
    export const Object_ = {
        TradingPosition: "trading_position",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
    export const Side = {
        Long: "long",
        Short: "short",
    } as const;
    export type Side = (typeof Side)[keyof typeof Side];
}
