// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

export interface Account {
    balances: Whop.AccountBalanceToken[];
    /** Account banner image URL. */
    banner_image_url: string | null;
    /** Account business address used to calculate tax, with `line1`, `line2`, `city`, `state`, `postal_code`, and `country`. `null` when no address is set. */
    business_address: Record<string, unknown> | null;
    /** The account's legal business name used with its tax address. */
    business_name: string | null;
    /** High-level business category for the account. See the [business types and industries glossary](/api-reference/beta/accounts/account#business-types-and-industries-glossary) for valid values. */
    business_type: Account.BusinessType | null;
    /** Whether pending funds may be transferred from this platform account to its connected accounts. */
    can_transfer_pending_balance_to_children: boolean;
    /** Payment rails enabled for this account, each `active`, `inactive`, or `pending` (onboarding or review in progress). Computed only on `retrieve` and `me` for callers with `company:balance:read` scope; `null` otherwise. */
    capabilities: Whop.AccountCapabilities | null;
    /** Whop Cards application details for the account. Computed only on `retrieve` and `me` for callers with `company:balance:read` scope; `null` otherwise, or when the account has no card application. */
    cards: Whop.AccountCards | null;
    /** Whether checkout shows a VAT/tax ID field for buyers to optionally enter. Does not require a VAT ID to purchase. */
    collect_vat_id: boolean;
    /** Company formation state for the account, managed through [Form Company](/api-reference/beta/accounts/form-company). A `draft` `status` until the formation checkout is paid, then filing progress with downloadable documents and signatures awaiting action. Empty when the formation state is temporarily unavailable. */
    company_formation: Whop.AccountCompanyFormation;
    /** Country where the account is located. */
    country: string | null;
    /** When the account was created, as an ISO 8601 timestamp. */
    created_at: string;
    /** Account promotional description. */
    description: string | null;
    /** Account owner email address. */
    email: string | null;
    home_preferences: Account.HomePreferences.Item[];
    /** Account ID, prefixed `biz_`. */
    id: string;
    /** Account industry group. See the [business types and industries glossary](/api-reference/beta/accounts/account#business-types-and-industries-glossary) for valid values. */
    industry_group: Account.IndustryGroup | null;
    /** Specific industry vertical for the account. See the [business types and industries glossary](/api-reference/beta/accounts/account#business-types-and-industries-glossary) for valid values. */
    industry_type: string | null;
    /** Prefix used for account invoices. */
    invoice_prefix: string | null;
    /** Account logo image URL. */
    logo_url: string | null;
    /** Arbitrary key/value metadata supplied at account creation. */
    metadata: Record<string, unknown>;
    /** Type of onboarding the account has completed. */
    onboarding_type: Account.OnboardingType | null;
    /** Account Open Graph image URL. */
    opengraph_image_url: string | null;
    /** Account Open Graph image variant. */
    opengraph_image_variant: Account.OpengraphImageVariant | null;
    /** Business type details when business_type is `other`. */
    other_business_description: string | null;
    /** Industry details when industry_type is `other`. */
    other_industry_description: string | null;
    /** The single user who owns the account, whose email is the `email` above. Distinct from the `owner` role on team members, which any number of them can hold. */
    owner: Whop.UserSummary;
    /** Parent account for connected accounts, or `null` for standalone accounts. */
    parent_account: Whop.AccountParent | null;
    /** Payment health controls currently applied to the account. Computed only on `retrieve` and `me` for callers with `company:balance:read` scope; `null` otherwise. */
    payment_controls: Whop.AccountPaymentControls | null;
    /** Tax classification code applied by default to the account's products, with `id`, `name`, and `product_type`. `null` when no default is set. */
    product_tax_code: Record<string, unknown> | null;
    /** DEPRECATED: Use the `GET /recommended_actions?account_id={account_id}` endpoint instead. */
    recommended_actions: Whop.AccountRecommendedAction[] | null;
    /** Whether authorized users must enable two-factor authentication. */
    require_2fa: boolean;
    required_actions: Whop.AccountRequiredAction[] | null;
    /** Account public route identifier. */
    route: string;
    /** Whether Whop sends transactional emails to customers on behalf of this account. */
    send_customer_emails: boolean;
    /** Whether the account appears in joined whops on other accounts. */
    show_joined_whops: boolean;
    /** Whether reviews are displayed on direct-to-consumer product pages. */
    show_reviews_dtc: boolean;
    /** Whether the account shows users in the user directory. */
    show_user_directory: boolean;
    social_links: Whop.AccountSocialLink[];
    /** Whether the account settles on stablecoin rails — its balance is held on-chain as USDT and paid out over crypto, rather than as fiat cash. */
    stablecoin_rails: boolean;
    /** Whether the account can operate on Whop: `active` or `suspended`. Computed on `list`, `retrieve`, and `me`; `null` otherwise. */
    status: string | null;
    /** Why the account was suspended, in language safe to show the account owner. Computed only on `retrieve` and `me`; `null` otherwise, when `status` is not `suspended`, and when the suspension was recorded without a reason. */
    status_reason: string | null;
    /** Account store page display configuration. */
    store_page_config: Whop.AccountStorePageConfig;
    /** Target audience for this account. */
    target_audience: string | null;
    tax_collection_enabled_states: string[];
    tax_identifiers: Whop.AccountTaxIdentifier[];
    /** Who calculates and remits tax for the account: `whop` (Whop calculates and remits), `self` (Whop calculates; the account collects and remits), or `none` (neither; the account is responsible). `null` until the account enrolls in the Whop tax service. */
    tax_remitted_by: Account.TaxRemittedBy | null;
    /** How tax is applied to the account's prices: `inclusive` (tax included in the listed price) or `exclusive` (tax added on top). Defaults to `exclusive` when unset; `null` only when the account has no payment connection. */
    tax_type: Account.TaxType | null;
    /** Account-level 3D Secure behavior. `mandate_challenge` requires cardholder verification on supported card payments; `null` uses the standard checkout flow. */
    three_ds_level: Account.ThreeDsLevel | null;
    /** Account display name. */
    title: string;
    /** Account lifetime sales, normalized to USD. Computed only on `retrieve` and `me` for callers with `stats:read` scope; `null` otherwise. */
    total_earned_usd: number | null;
    /** Total USD value across balances with known exchange rates. Computed only on single-account reads (`retrieve` and `me`); `null` on list responses, writes, missing balance-read permission, or unavailable balance source. */
    total_usd: string | null;
    /** Whether the account uses its logo as the fallback Open Graph image. */
    use_logo_as_opengraph_image_fallback: boolean;
    /** Account identity verification status for the `individual` (KYC) and `business` (KYB) profiles. Each is `null` until created, otherwise a `status` of `not_started`, `pending`, `manual_review`, `approved`, or `rejected`. */
    verification: Record<string, unknown>;
    /** Lifetime volume through the account — sales plus transfers received — normalized to USD. Computed only on `list` for callers with `stats:read` on the account; `null` otherwise. */
    volume_usd: number | null;
    /** Account primary crypto wallet, or `null` if none has been provisioned. */
    wallet: Whop.AccountWallet | null;
}

export namespace Account {
    /** High-level business category for the account. See the [business types and industries glossary](/api-reference/beta/accounts/account#business-types-and-industries-glossary) for valid values. */
    export const BusinessType = {
        EducationProgram: "education_program",
        Coaching: "coaching",
        Software: "software",
        PaidGroup: "paid_group",
        Newsletter: "newsletter",
        Agency: "agency",
        PhysicalProducts: "physical_products",
        BrickAndMortar: "brick_and_mortar",
        Events: "events",
        CoachingAndCourses: "coaching_and_courses",
        Other: "other",
        Services: "services",
        GigEconomy: "gig_economy",
        Marketplace: "marketplace",
        Telehealth: "telehealth",
        ClassActionSettlement: "class_action_settlement",
        PhysicalProduct: "physical_product",
        Saas: "saas",
        Course: "course",
        Community: "community",
    } as const;
    export type BusinessType = (typeof BusinessType)[keyof typeof BusinessType];
    export type HomePreferences = HomePreferences.Item[];

    export namespace HomePreferences {
        /** Public account home page preferences. */
        export const Item = {
            HideMemberCount: "hide_member_count",
            HideMembersCard: "hide_members_card",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    /** Account industry group. See the [business types and industries glossary](/api-reference/beta/accounts/account#business-types-and-industries-glossary) for valid values. */
    export const IndustryGroup = {
        AcademicAndTestPrep: "academic_and_test_prep",
        Accessories: "accessories",
        AgricultureAndFarming: "agriculture_and_farming",
        AiAndAutomationAgencies: "ai_and_automation_agencies",
        AiAndAutomationSoftware: "ai_and_automation_software",
        ArtsAndCrafts: "arts_and_crafts",
        Automotive: "automotive",
        B2BAndProfessionalMarketplaces: "b2b_and_professional_marketplaces",
        BabyAndKids: "baby_and_kids",
        BarsAndBreweries: "bars_and_breweries",
        BeautyAndPersonalCare: "beauty_and_personal_care",
        BeautyAndWellness: "beauty_and_wellness",
        BusinessAndEntrepreneurship: "business_and_entrepreneurship",
        BusinessAndMoneyGroups: "business_and_money_groups",
        CafesAndQuickService: "cafes_and_quick_service",
        CareerAndProfessional: "career_and_professional",
        CharityAndCauseEvents: "charity_and_cause_events",
        ClassActionSettlement: "class_action_settlement",
        ClothingAndApparel: "clothing_and_apparel",
        CommunicationAndMessagingSoftware: "communication_and_messaging_software",
        CommunityAndEducationSoftware: "community_and_education_software",
        ConferenceAndExpoEvents: "conference_and_expo_events",
        Consulting: "consulting",
        ContentAndClippingAgencies: "content_and_clipping_agencies",
        CreativeAndContentCreation: "creative_and_content_creation",
        CreativeAndContentGroups: "creative_and_content_groups",
        CreativeAndEducation: "creative_and_education",
        CreativeGigs: "creative_gigs",
        CreativeServices: "creative_services",
        CustomerSupportAgencies: "customer_support_agencies",
        DatingAndRelationships: "dating_and_relationships",
        DeliveryAndLogistics: "delivery_and_logistics",
        DentalAndVision: "dental_and_vision",
        DermatologyAndSkin: "dermatology_and_skin",
        DesignAndCreativeAgencies: "design_and_creative_agencies",
        DeveloperAndTechnicalTools: "developer_and_technical_tools",
        DevelopmentAgencies: "development_agencies",
        DigitalAndEducationMarketplaces: "digital_and_education_marketplaces",
        DigitalGoodsAndAccounts: "digital_goods_and_accounts",
        ECommerceSoftware: "e_commerce_software",
        EducationAndChildcare: "education_and_childcare",
        EducationalTrainingEvents: "educational_training_events",
        ElectronicsAndGadgets: "electronics_and_gadgets",
        EntertainmentAndLeisure: "entertainment_and_leisure",
        FamilyAndCommunityEvents: "family_and_community_events",
        FinanceAndInvesting: "finance_and_investing",
        FitnessAndAthletics: "fitness_and_athletics",
        FitnessAndHealthGroups: "fitness_and_health_groups",
        FitnessAndRecreation: "fitness_and_recreation",
        FitnessEquipmentAndGear: "fitness_equipment_and_gear",
        FoodAndBeverages: "food_and_beverages",
        FoodAndHospitalityMarketplaces: "food_and_hospitality_marketplaces",
        FuneralAndDeathCare: "funeral_and_death_care",
        GamingAndEntertainmentSoftware: "gaming_and_entertainment_software",
        GamingGroups: "gaming_groups",
        GeneticAndSpecialized: "genetic_and_specialized",
        GovernmentAndPublic: "government_and_public",
        HealthAndWellness: "health_and_wellness",
        HealthAndWellnessServices: "health_and_wellness_services",
        Healthcare: "healthcare",
        HealthcareAndWellnessSoftware: "healthcare_and_wellness_software",
        HobbiesAndLifestyle: "hobbies_and_lifestyle",
        HobbyAndInterestGroups: "hobby_and_interest_groups",
        HomeAndLiving: "home_and_living",
        HomeAndTradeServices: "home_and_trade_services",
        HomeAndTradeStorefronts: "home_and_trade_storefronts",
        HomeImprovementAndTools: "home_improvement_and_tools",
        HomeServicesGigs: "home_services_gigs",
        HospitalityAndLodging: "hospitality_and_lodging",
        IndustrialAndManufacturing: "industrial_and_manufacturing",
        IndustrySpecificSoftware: "industry_specific_software",
        LanguageAndCommunication: "language_and_communication",
        LegalAndCompliance: "legal_and_compliance",
        LifestyleAndCulture: "lifestyle_and_culture",
        LifestyleAndPersonalGrowth: "lifestyle_and_personal_growth",
        LifestyleAndPersonalGrowthGroups: "lifestyle_and_personal_growth_groups",
        LifestyleAndWellnessEvents: "lifestyle_and_wellness_events",
        LogisticsAndTransportationServices: "logistics_and_transportation_services",
        MarketingAgencies: "marketing_agencies",
        MarketingAndAdvertising: "marketing_and_advertising",
        MarketingAndSalesSoftware: "marketing_and_sales_software",
        MediaAndPublishingCompanies: "media_and_publishing_companies",
        MentalHealthAndBehavioral: "mental_health_and_behavioral",
        Miscellaneous: "miscellaneous",
        MusicAndPerformingArts: "music_and_performing_arts",
        NewsAndPolitics: "news_and_politics",
        NonprofitAndCharity: "nonprofit_and_charity",
        OfficeAndBusinessSupplies: "office_and_business_supplies",
        OutdoorAndSports: "outdoor_and_sports",
        PerformanceAndShowEvents: "performance_and_show_events",
        PersonalDevelopment: "personal_development",
        PersonalFinance: "personal_finance",
        PersonalServices: "personal_services",
        PetServices: "pet_services",
        PetsAndAnimals: "pets_and_animals",
        PrimaryAndGeneralCare: "primary_and_general_care",
        ProductMarketplaces: "product_marketplaces",
        ProductivityAndBusinessOps: "productivity_and_business_ops",
        ProfessionalGigs: "professional_gigs",
        ProfessionalServices: "professional_services",
        ProfessionalServicesStorefront: "professional_services_storefront",
        PublishingAndInfoProducts: "publishing_and_info_products",
        RealEstate: "real_estate",
        RealEstateSoftware: "real_estate_software",
        RecruitingAndStaffing: "recruiting_and_staffing",
        RehabilitationAndTherapy: "rehabilitation_and_therapy",
        ReligionAndFaith: "religion_and_faith",
        RentalMarketplaces: "rental_marketplaces",
        Restaurants: "restaurants",
        Retail: "retail",
        SalesAgencies: "sales_agencies",
        SalesAndRevenue: "sales_and_revenue",
        SecurityAndInvestigations: "security_and_investigations",
        SecurityAndPrivacySoftware: "security_and_privacy_software",
        ServiceMarketplaces: "service_marketplaces",
        SleepAndChronicConditions: "sleep_and_chronic_conditions",
        SocialAndNetworkingEvents: "social_and_networking_events",
        SocialEntertainmentEvents: "social_entertainment_events",
        SpecializedGigs: "specialized_gigs",
        SpecialtyMedicalCare: "specialty_medical_care",
        SpiritualityAndMindfulness: "spirituality_and_mindfulness",
        SpiritualityAndPersonalGrowth: "spirituality_and_personal_growth",
        SportsAndFitnessEvents: "sports_and_fitness_events",
        SportsBettingAndGambling: "sports_betting_and_gambling",
        SportsBettingGroups: "sports_betting_groups",
        SupplementsAndNutrition: "supplements_and_nutrition",
        SustainabilityAndEcoProducts: "sustainability_and_eco_products",
        TaskAndErrands: "task_and_errands",
        TechAndAi: "tech_and_ai",
        TechAndDevGroups: "tech_and_dev_groups",
        TechAndDevelopment: "tech_and_development",
        TradingAndFinanceSoftware: "trading_and_finance_software",
        TradingAndInvesting: "trading_and_investing",
        TradingAndInvestingGroups: "trading_and_investing_groups",
        Transportation: "transportation",
        Veterinary: "veterinary",
        VideoGamesAndEsports: "video_games_and_esports",
        WeightAndMetabolicHealth: "weight_and_metabolic_health",
        WellnessAndAlternative: "wellness_and_alternative",
        WomensAndMensHealth: "womens_and_mens_health",
    } as const;
    export type IndustryGroup = (typeof IndustryGroup)[keyof typeof IndustryGroup];
    /** Type of onboarding the account has completed. */
    export const OnboardingType = {
        Platform: "platform",
        Seller: "seller",
    } as const;
    export type OnboardingType = (typeof OnboardingType)[keyof typeof OnboardingType];
    /** Account Open Graph image variant. */
    export const OpengraphImageVariant = {
        White: "white",
        Black: "black",
        Orange: "orange",
    } as const;
    export type OpengraphImageVariant = (typeof OpengraphImageVariant)[keyof typeof OpengraphImageVariant];
    /** Who calculates and remits tax for the account: `whop` (Whop calculates and remits), `self` (Whop calculates; the account collects and remits), or `none` (neither; the account is responsible). `null` until the account enrolls in the Whop tax service. */
    export const TaxRemittedBy = {
        Whop: "whop",
        Self: "self",
        None: "none",
    } as const;
    export type TaxRemittedBy = (typeof TaxRemittedBy)[keyof typeof TaxRemittedBy];
    /** How tax is applied to the account's prices: `inclusive` (tax included in the listed price) or `exclusive` (tax added on top). Defaults to `exclusive` when unset; `null` only when the account has no payment connection. */
    export const TaxType = {
        Inclusive: "inclusive",
        Exclusive: "exclusive",
    } as const;
    export type TaxType = (typeof TaxType)[keyof typeof TaxType];
    /** Account-level 3D Secure behavior. `mandate_challenge` requires cardholder verification on supported card payments; `null` uses the standard checkout flow. */
    export const ThreeDsLevel = {
        MandateChallenge: "mandate_challenge",
    } as const;
    export type ThreeDsLevel = (typeof ThreeDsLevel)[keyof typeof ThreeDsLevel];
}
