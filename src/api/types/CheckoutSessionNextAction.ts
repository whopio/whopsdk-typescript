// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../index.js";

/**
 * The ceremony to run. `type` picks the shape, and each type carries only the fields it needs, so switching on `type` gives you exactly that action's payload. Every type carries `blocking`, which says whether the sale depends on this one.
 */
export type CheckoutSessionNextAction =
    | Whop.CheckoutSessionNextAction.Authenticate
    | Whop.CheckoutSessionNextAction.AwaitClaim
    | Whop.CheckoutSessionNextAction.Complete
    | Whop.CheckoutSessionNextAction.Redirect
    | Whop.CheckoutSessionNextAction.UpgradeAuthentication
    | Whop.CheckoutSessionNextAction.VerifyPhone
    | Whop.CheckoutSessionNextAction.WaitForPayment;

export namespace CheckoutSessionNextAction {
    export interface Authenticate extends Whop.CheckoutSessionAuthenticateAction {
        type: "authenticate";
    }

    export interface AwaitClaim extends Whop.CheckoutSessionAwaitClaimAction {
        type: "await_claim";
    }

    export interface Complete extends Whop.CheckoutSessionCompleteAction {
        type: "complete";
    }

    export interface Redirect extends Whop.CheckoutSessionRedirectAction {
        type: "redirect";
    }

    export interface UpgradeAuthentication extends Whop.CheckoutSessionUpgradeAuthenticationAction {
        type: "upgrade_authentication";
    }

    export interface VerifyPhone extends Whop.CheckoutSessionVerifyPhoneAction {
        type: "verify_phone";
    }

    export interface WaitForPayment extends Whop.CheckoutSessionWaitForPaymentAction {
        type: "wait_for_payment";
    }
}
