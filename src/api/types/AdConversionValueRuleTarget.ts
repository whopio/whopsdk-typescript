// This file was auto-generated by Fern from our API Definition.

export interface AdConversionValueRuleTarget {
    /** Ad platform that receives the adjusted value. */
    platform: AdConversionValueRuleTarget.Platform;
    /** Selected campaign, ad group, or ad ID. Null for a business target. */
    resource_id: string | null;
    /** Name of the selected item. Null when it is no longer available. */
    resource_title: string | null;
    /** Business, campaign, ad group, or ad covered by this target. */
    scope: AdConversionValueRuleTarget.Scope;
}

export namespace AdConversionValueRuleTarget {
    /** Ad platform that receives the adjusted value. */
    export const Platform = {
        Tiktok: "tiktok",
        Meta: "meta",
        Google: "google",
    } as const;
    export type Platform = (typeof Platform)[keyof typeof Platform];
    /** Business, campaign, ad group, or ad covered by this target. */
    export const Scope = {
        Business: "business",
        Campaign: "campaign",
        AdGroup: "ad_group",
        Ad: "ad",
    } as const;
    export type Scope = (typeof Scope)[keyof typeof Scope];
}
