// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         account_id: "account_id"
 *     }
 */
export interface UpdateFeesRequest {
    /** Account ID, prefixed `biz_`. */
    account_id: string;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    ads?: UpdateFeesRequest.Ads;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    bank_deposit?: UpdateFeesRequest.BankDeposit;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    billing?: UpdateFeesRequest.Billing;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    buyer?: UpdateFeesRequest.Buyer;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    card_processing?: UpdateFeesRequest.CardProcessing;
    /** This platform's default markups for every account connected to it. */
    child_markups?: UpdateFeesRequest.ChildMarkups;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    cross_border?: UpdateFeesRequest.CrossBorder;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    dispute?: UpdateFeesRequest.Dispute;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    dispute_alert?: UpdateFeesRequest.DisputeAlert;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    dispute_alert_cdrn?: UpdateFeesRequest.DisputeAlertCdrn;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    dispute_alert_ethoca?: UpdateFeesRequest.DisputeAlertEthoca;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    dispute_alert_rdr?: UpdateFeesRequest.DisputeAlertRdr;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    dispute_representment?: UpdateFeesRequest.DisputeRepresentment;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    foreign_exchange?: UpdateFeesRequest.ForeignExchange;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    fraud_screening?: UpdateFeesRequest.FraudScreening;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    high_risk?: UpdateFeesRequest.HighRisk;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    marketplace?: UpdateFeesRequest.Marketplace;
    /** Markups on this connected account, set by the platform it is connected to. */
    markups?: UpdateFeesRequest.Markups;
    /** Why the fees are changing, recorded with the change. Required when a Whop Verified Partner edits the fee schedule; ignored for markups. */
    notes?: string;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    orchestration?: UpdateFeesRequest.Orchestration;
    /** Changes to non-card payment method fees, keyed by payment method type. */
    payment_methods?: Record<string, UpdateFeesRequest.PaymentMethods.Value>;
    /** Changes to withdrawal fees, keyed by payout method. */
    payouts?: Record<string, UpdateFeesRequest.Payouts.Value>;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    pending_auto_topup?: UpdateFeesRequest.PendingAutoTopup;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    platform_processing?: UpdateFeesRequest.PlatformProcessing;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    pool_payout?: UpdateFeesRequest.PoolPayout;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    revshare?: UpdateFeesRequest.Revshare;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    tax_calculation?: UpdateFeesRequest.TaxCalculation;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    tax_service?: UpdateFeesRequest.TaxService;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    three_ds?: UpdateFeesRequest.ThreeDs;
    /** The fields of a fee the caller may change. Only the keys sent are replaced. */
    transfers?: UpdateFeesRequest.Transfers;
}

export namespace UpdateFeesRequest {
    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface Ads {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, Ads.Regions.Value> | undefined;
    }

    export namespace Ads {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface BankDeposit {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, BankDeposit.Regions.Value> | undefined;
    }

    export namespace BankDeposit {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface Billing {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, Billing.Regions.Value> | undefined;
    }

    export namespace Billing {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface Buyer {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, Buyer.Regions.Value> | undefined;
    }

    export namespace Buyer {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface CardProcessing {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, CardProcessing.Regions.Value> | undefined;
    }

    export namespace CardProcessing {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * This platform's default markups for every account connected to it.
     */
    export interface ChildMarkups {
        /** The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default. */
        crypto_swaps?: (ChildMarkups.CryptoSwaps | null) | undefined;
        /** Markups on deposits, keyed by rail: `bank` or `crypto`. */
        deposits?: Record<string, ChildMarkups.Deposits.Value | null> | undefined;
        /** The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default. */
        payments?: (ChildMarkups.Payments | null) | undefined;
        /** Markups on withdrawals, keyed by payout method. */
        payouts?: Record<string, ChildMarkups.Payouts.Value | null> | undefined;
        /** The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default. */
        transfers?: (ChildMarkups.Transfers | null) | undefined;
    }

    export namespace ChildMarkups {
        /**
         * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
         */
        export interface CryptoSwaps {
            /** The amount the platform adds per event, in US dollars. */
            fixed?: number | undefined;
            /** The percentage of the transaction the platform adds, where `2` means 2%. */
            percentage?: number | undefined;
        }

        export namespace Deposits {
            /**
             * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
             */
            export interface Value {
                /** The amount the platform adds per event, in US dollars. */
                fixed?: number | undefined;
                /** The percentage of the transaction the platform adds, where `2` means 2%. */
                percentage?: number | undefined;
            }
        }

        /**
         * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
         */
        export interface Payments {
            /** The amount the platform adds per event, in US dollars. */
            fixed?: number | undefined;
            /** The percentage of the transaction the platform adds, where `2` means 2%. */
            percentage?: number | undefined;
        }

        export namespace Payouts {
            /**
             * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
             */
            export interface Value {
                /** The amount the platform adds per event, in US dollars. */
                fixed?: number | undefined;
                /** The percentage of the transaction the platform adds, where `2` means 2%. */
                percentage?: number | undefined;
            }
        }

        /**
         * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
         */
        export interface Transfers {
            /** The amount the platform adds per event, in US dollars. */
            fixed?: number | undefined;
            /** The percentage of the transaction the platform adds, where `2` means 2%. */
            percentage?: number | undefined;
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface CrossBorder {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, CrossBorder.Regions.Value> | undefined;
    }

    export namespace CrossBorder {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface Dispute {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, Dispute.Regions.Value> | undefined;
    }

    export namespace Dispute {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface DisputeAlert {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, DisputeAlert.Regions.Value> | undefined;
    }

    export namespace DisputeAlert {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface DisputeAlertCdrn {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, DisputeAlertCdrn.Regions.Value> | undefined;
    }

    export namespace DisputeAlertCdrn {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface DisputeAlertEthoca {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, DisputeAlertEthoca.Regions.Value> | undefined;
    }

    export namespace DisputeAlertEthoca {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface DisputeAlertRdr {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, DisputeAlertRdr.Regions.Value> | undefined;
    }

    export namespace DisputeAlertRdr {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface DisputeRepresentment {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, DisputeRepresentment.Regions.Value> | undefined;
    }

    export namespace DisputeRepresentment {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface ForeignExchange {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, ForeignExchange.Regions.Value> | undefined;
    }

    export namespace ForeignExchange {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface FraudScreening {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, FraudScreening.Regions.Value> | undefined;
    }

    export namespace FraudScreening {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface HighRisk {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, HighRisk.Regions.Value> | undefined;
    }

    export namespace HighRisk {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface Marketplace {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, Marketplace.Regions.Value> | undefined;
    }

    export namespace Marketplace {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * Markups on this connected account, set by the platform it is connected to.
     */
    export interface Markups {
        /** The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default. */
        crypto_swaps?: (Markups.CryptoSwaps | null) | undefined;
        /** Markups on deposits, keyed by rail: `bank` or `crypto`. */
        deposits?: Record<string, Markups.Deposits.Value | null> | undefined;
        /** The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default. */
        payments?: (Markups.Payments | null) | undefined;
        /** Markups on withdrawals, keyed by payout method. */
        payouts?: Record<string, Markups.Payouts.Value | null> | undefined;
        /** The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default. */
        transfers?: (Markups.Transfers | null) | undefined;
    }

    export namespace Markups {
        /**
         * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
         */
        export interface CryptoSwaps {
            /** The amount the platform adds per event, in US dollars. */
            fixed?: number | undefined;
            /** The percentage of the transaction the platform adds, where `2` means 2%. */
            percentage?: number | undefined;
        }

        export namespace Deposits {
            /**
             * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
             */
            export interface Value {
                /** The amount the platform adds per event, in US dollars. */
                fixed?: number | undefined;
                /** The percentage of the transaction the platform adds, where `2` means 2%. */
                percentage?: number | undefined;
            }
        }

        /**
         * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
         */
        export interface Payments {
            /** The amount the platform adds per event, in US dollars. */
            fixed?: number | undefined;
            /** The percentage of the transaction the platform adds, where `2` means 2%. */
            percentage?: number | undefined;
        }

        export namespace Payouts {
            /**
             * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
             */
            export interface Value {
                /** The amount the platform adds per event, in US dollars. */
                fixed?: number | undefined;
                /** The percentage of the transaction the platform adds, where `2` means 2%. */
                percentage?: number | undefined;
            }
        }

        /**
         * The new markup. Fields left out keep their current value; `null` clears the row so the markup returns to its default.
         */
        export interface Transfers {
            /** The amount the platform adds per event, in US dollars. */
            fixed?: number | undefined;
            /** The percentage of the transaction the platform adds, where `2` means 2%. */
            percentage?: number | undefined;
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface Orchestration {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, Orchestration.Regions.Value> | undefined;
    }

    export namespace Orchestration {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    export namespace PaymentMethods {
        /**
         * The fields of a fee the caller may change. Only the keys sent are replaced.
         */
        export interface Value {
            /** The new amount per event in US dollars. `null` clears the custom amount. */
            fixed?: (number | null) | undefined;
            /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
            percentage?: (number | null) | undefined;
            /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
            regions?: Record<string, Value.Regions.Value> | undefined;
        }

        export namespace Value {
            export namespace Regions {
                export interface Value {
                    /** The new amount per event in US dollars. `null` clears the custom amount. */
                    fixed?: (number | null) | undefined;
                    /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                    percentage?: (number | null) | undefined;
                }
            }
        }
    }

    export namespace Payouts {
        /**
         * The fields of a fee the caller may change. Only the keys sent are replaced.
         */
        export interface Value {
            /** The new amount per event in US dollars. `null` clears the custom amount. */
            fixed?: (number | null) | undefined;
            /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
            percentage?: (number | null) | undefined;
            /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
            regions?: Record<string, Value.Regions.Value> | undefined;
        }

        export namespace Value {
            export namespace Regions {
                export interface Value {
                    /** The new amount per event in US dollars. `null` clears the custom amount. */
                    fixed?: (number | null) | undefined;
                    /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                    percentage?: (number | null) | undefined;
                }
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface PendingAutoTopup {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, PendingAutoTopup.Regions.Value> | undefined;
    }

    export namespace PendingAutoTopup {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface PlatformProcessing {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, PlatformProcessing.Regions.Value> | undefined;
    }

    export namespace PlatformProcessing {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface PoolPayout {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, PoolPayout.Regions.Value> | undefined;
    }

    export namespace PoolPayout {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface Revshare {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, Revshare.Regions.Value> | undefined;
    }

    export namespace Revshare {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface TaxCalculation {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, TaxCalculation.Regions.Value> | undefined;
    }

    export namespace TaxCalculation {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface TaxService {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, TaxService.Regions.Value> | undefined;
    }

    export namespace TaxService {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface ThreeDs {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, ThreeDs.Regions.Value> | undefined;
    }

    export namespace ThreeDs {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }

    /**
     * The fields of a fee the caller may change. Only the keys sent are replaced.
     */
    export interface Transfers {
        /** The new amount per event in US dollars. `null` clears the custom amount. */
        fixed?: (number | null) | undefined;
        /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
        percentage?: (number | null) | undefined;
        /** Changes for the other regions the fee varies by, keyed by region. Only accepted on a fee whose `regions` is non-empty. */
        regions?: Record<string, Transfers.Regions.Value> | undefined;
    }

    export namespace Transfers {
        export namespace Regions {
            export interface Value {
                /** The new amount per event in US dollars. `null` clears the custom amount. */
                fixed?: (number | null) | undefined;
                /** The new percentage, where `2` means 2%. `null` clears the custom rate so the fee returns to its default or inherited rate. */
                percentage?: (number | null) | undefined;
            }
        }
    }
}
