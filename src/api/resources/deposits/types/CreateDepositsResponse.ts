// This file was auto-generated by Fern from our API Definition.

export interface CreateDepositsResponse {
    /** Account ID of the destination owner. Null for raw wallet address destinations. */
    account_id: string | null;
    /** Requested deposit amount. */
    amount?: string | undefined;
    /** URL of the hosted deposit page. Only present for business destinations. */
    hosted_url: string | null;
    /** Metadata from the request. */
    metadata: Record<string, unknown>;
    /** Available deposit methods for destination. */
    methods: CreateDepositsResponse.Methods;
    object: CreateDepositsResponse.Object_;
}

export namespace CreateDepositsResponse {
    /**
     * Available deposit methods for destination.
     */
    export interface Methods {
        /** Bank deposit details. Only present when bank deposits are active for the destination account. */
        bank: Methods.Bank | null;
        /** Crypto networks available for this deposit, each with its on-chain deposit address and the tokens accepted on that network. */
        crypto: Methods.Crypto.Item[];
    }

    export namespace Methods {
        /**
         * Bank deposit details. Only present when bank deposits are active for the destination account.
         */
        export interface Bank {
            /** Bank transfer currencies available for this deposit. */
            currencies: Bank.Currencies.Item[];
        }

        export namespace Bank {
            export type Currencies = Currencies.Item[];

            export namespace Currencies {
                export interface Item {
                    /** Bank account number for deposits in this currency. */
                    account_number: string | null;
                    /** Currency supported by these bank instructions. */
                    currency: string;
                    /** Receiving bank address. */
                    deposit_bank_address: string | null;
                    /** Receiving bank name. */
                    deposit_bank_name: string | null;
                    /** Beneficiary name to use for transfer. */
                    deposit_beneficiary_name: string | null;
                    /** Reference to include with bank transfer. */
                    deposit_reference: string | null;
                    /** Active deposit rails for this currency. */
                    rails: Item.Rails.Item[];
                    /** Bank routing number for deposits in this currency. */
                    routing_number: string | null;
                    /** SWIFT/BIC code for international wires, when available. */
                    swift_bic: string | null;
                }

                export namespace Item {
                    export type Rails = Rails.Item[];

                    export namespace Rails {
                        export const Item = {
                            Ach: "ach",
                            Wire: "wire",
                            Sepa: "sepa",
                            Fps: "fps",
                            Chaps: "chaps",
                        } as const;
                        export type Item = (typeof Item)[keyof typeof Item];
                    }
                }
            }
        }

        export type Crypto = Crypto.Item[];

        export namespace Crypto {
            export interface Item {
                /** Address to send funds to on this network. Null when the provider has not issued one yet. */
                deposit_address: string | null;
                /** Network icon URL. */
                icon_url: string | null;
                /** Network display name. */
                name: Item.Name;
                /** Tokens accepted for deposit on this network. */
                supported_currencies: Item.SupportedCurrencies.Item[];
            }

            export namespace Item {
                /** Network display name. */
                export const Name = {
                    Ethereum: "Ethereum",
                    Solana: "Solana",
                    Base: "Base",
                    BnbSmartChain: "BNB Smart Chain",
                    Hyperliquid: "Hyperliquid",
                    Hypercore: "Hypercore",
                    MegaEth: "MegaETH",
                    Polygon: "Polygon",
                    Plasma: "Plasma",
                    Arbitrum: "Arbitrum",
                } as const;
                export type Name = (typeof Name)[keyof typeof Name];
                export type SupportedCurrencies = SupportedCurrencies.Item[];

                export namespace SupportedCurrencies {
                    export interface Item {
                        /** Token icon URL. Null when no icon is available. */
                        icon_url: string | null;
                        /** Token symbol. */
                        name: Item.Name;
                    }

                    export namespace Item {
                        /** Token symbol. */
                        export const Name = {
                            Arb: "ARB",
                            Bnb: "BNB",
                            Eth: "ETH",
                            Eurc: "EURC",
                            Hype: "HYPE",
                            Pyusd: "PYUSD",
                            Sol: "SOL",
                            Usd1: "USD1",
                            Usdc: "USDC",
                            UsdcE: "USDC.e",
                            Usdg: "USDG",
                            Usdt: "USDT",
                            Usdt0: "USDT0",
                            UsDe: "USDe",
                            UsDm: "USDm",
                            Xo: "XO",
                            Xpl: "XPL",
                            PUsd: "pUSD",
                            WEth: "wETH",
                        } as const;
                        export type Name = (typeof Name)[keyof typeof Name];
                    }
                }
            }
        }
    }

    export const Object_ = {
        Deposit: "deposit",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
