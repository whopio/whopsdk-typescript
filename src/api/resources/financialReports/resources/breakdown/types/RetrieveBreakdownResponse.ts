// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../../../../../index.js";

export interface RetrieveBreakdownResponse {
    activity_filters: RetrieveBreakdownResponse.ActivityFilters;
    bucket: RetrieveBreakdownResponse.Bucket;
    currency: string;
    direction: RetrieveBreakdownResponse.Direction;
    items: RetrieveBreakdownResponse.Items.Item[];
    other_amount: Whop.Money | null;
}

export namespace RetrieveBreakdownResponse {
    export interface ActivityFilters {
        direction: ActivityFilters.Direction;
        line_types: string[];
    }

    export namespace ActivityFilters {
        export const Direction = {
            MoneyIn: "money_in",
            MoneyOut: "money_out",
        } as const;
        export type Direction = (typeof Direction)[keyof typeof Direction];
    }

    export const Bucket = {
        Transfers: "transfers",
        Payments: "payments",
        CardSpend: "card_spend",
        Withdrawals: "withdrawals",
        Swaps: "swaps",
    } as const;
    export type Bucket = (typeof Bucket)[keyof typeof Bucket];
    export const Direction = {
        MoneyIn: "money_in",
        MoneyOut: "money_out",
    } as const;
    export type Direction = (typeof Direction)[keyof typeof Direction];
    export type Items = Items.Item[];

    export namespace Items {
        export interface Item {
            amount: Whop.Money;
            image_url: string | null;
            /** An opaque identifier for this grouping within the breakdown. */
            key: string;
            name: string;
            object: Item.Object_;
            /** The prefixed identifier of the represented Whop resource, when one exists. */
            resource_id: string | null;
        }

        export namespace Item {
            export const Object_ = {
                User: "user",
                Account: "account",
                Merchant: "merchant",
                PayoutDestination: "payout_destination",
                Balance: "balance",
            } as const;
            export type Object_ = (typeof Object_)[keyof typeof Object_];
        }
    }
}
