// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         account_id: "biz_xxxxxxxxxxxxxx",
 *         leverage: 5,
 *         margin_mode: "cross",
 *         market: "ETH",
 *         provider: "hyperliquid"
 *     }
 */
export interface UpdateLeverageTradesRequest {
    /** The account or user that owns the trading wallet, prefixed `biz_` or `user_`. */
    account_id: string;
    /** Leverage multiplier, such as `10` for 10x. Capped at the market's maximum. */
    leverage: number;
    /** `cross` shares margin across positions; `isolated` limits margin to this market's position. */
    margin_mode: UpdateLeverageTradesRequest.MarginMode;
    /** Perpetual market on the provider, such as `ETH`. */
    market: string;
    /** Free-form string-to-string annotations stored on the trade. */
    metadata?: Record<string, string | null> | null;
    provider: UpdateLeverageTradesRequest.Provider;
}

export namespace UpdateLeverageTradesRequest {
    /** `cross` shares margin across positions; `isolated` limits margin to this market's position. */
    export const MarginMode = {
        Cross: "cross",
        Isolated: "isolated",
    } as const;
    export type MarginMode = (typeof MarginMode)[keyof typeof MarginMode];
    export const Provider = {
        Hyperliquid: "hyperliquid",
    } as const;
    export type Provider = (typeof Provider)[keyof typeof Provider];
}
