// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         id: "id",
 *         requested_permissions: [{
 *                 action: "company:basic:read",
 *                 is_required: true,
 *                 justification: "Reads basic account info to render the dashboard home."
 *             }]
 *     }
 */
export interface UpdatePermissionsAppsRequest {
    /** App ID, prefixed `app_`. */
    id: string;
    /** The full set of permissions the app requests on install; permissions not listed are removed. */
    requested_permissions: UpdatePermissionsAppsRequest.RequestedPermissions.Item[];
}

export namespace UpdatePermissionsAppsRequest {
    export type RequestedPermissions = RequestedPermissions.Item[];

    export namespace RequestedPermissions {
        export interface Item {
            /** The permission action, for example `company:basic:read`. */
            action: string;
            /** Whether installing the app requires granting this permission. */
            is_required: boolean;
            /** Why the app needs this permission (20-512 characters), shown to the installing user. */
            justification: string;
        }
    }
}
