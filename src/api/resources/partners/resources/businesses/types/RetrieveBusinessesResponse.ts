// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../../../../../index.js";

export interface RetrieveBusinessesResponse {
    /** Referred account. */
    account: RetrieveBusinessesResponse.Account | null;
    /** When the partner business was created. */
    created_at: string;
    earnings_usd: RetrieveBusinessesResponse.EarningsUsd;
    /** The partner who referred the business owner onto Whop (first tier). Null if there is no active first-tier partner. */
    first_tier_partner: RetrieveBusinessesResponse.FirstTierPartner | null;
    /** Partner business ID. */
    id: string;
    /** Which tier the caller earns on for this business: `first` (they referred the owner) or `second` (they referred the first-tier partner). */
    my_partner_tier: RetrieveBusinessesResponse.MyPartnerTier;
    object: RetrieveBusinessesResponse.Object_;
    /** The owner of the referred business. */
    owner: RetrieveBusinessesResponse.Owner | null;
    /** The referrer's commission rate for each income source, expressed as a fraction (0.3 = 30%). */
    payout_percentages: RetrieveBusinessesResponse.PayoutPercentages;
    /** When the referral expires. */
    referral_expires_at: string | null;
    /** When the referral became active. */
    referral_started_at: string | null;
    /** The second-tier partner who earns on this business (referred the first-tier partner). Null if there is no active second-tier partner. */
    second_tier_partner: RetrieveBusinessesResponse.SecondTierPartner | null;
    /** Current referral status. */
    status: RetrieveBusinessesResponse.Status;
    volume_usd: RetrieveBusinessesResponse.VolumeUsd;
}

export namespace RetrieveBusinessesResponse {
    /**
     * Referred account.
     */
    export interface Account {
        capabilities: Whop.AccountCapabilities;
        /** Referred account ID. */
        id: string;
        /** Referred account logo URL. */
        logo_url: string | null;
        /** Optional actions that unlock capabilities or grow the referred account. */
        recommended_actions: Whop.AccountRecommendedAction[] | null;
        /** Actions the referred account owner must take to unblock capabilities. */
        required_actions: Whop.AccountRequiredAction[] | null;
        /** Referred account route. */
        route: string;
        /** Referred account display name. */
        title: string;
    }

    export interface EarningsUsd {
        /** Commission already paid out, in USD. */
        completed: string;
        /** Commission scheduled but not yet paid, in USD. */
        pending: string;
        /** Pending + completed commission, in USD. */
        total: string;
    }

    /**
     * The partner who referred the business owner onto Whop (first tier). Null if there is no active first-tier partner.
     */
    export interface FirstTierPartner {
        /** User ID, prefixed `user_`. */
        id: string;
        /** The user's display name. */
        name: string | null;
        /** The user's profile picture. */
        profile_picture: FirstTierPartner.ProfilePicture;
        /** The user's unique username. */
        username: string;
    }

    export namespace FirstTierPartner {
        /**
         * The user's profile picture.
         */
        export interface ProfilePicture {
            /** The user's profile picture URL. */
            url: string;
        }
    }

    /** Which tier the caller earns on for this business: `first` (they referred the owner) or `second` (they referred the first-tier partner). */
    export const MyPartnerTier = {
        First: "first",
        Second: "second",
    } as const;
    export type MyPartnerTier = (typeof MyPartnerTier)[keyof typeof MyPartnerTier];
    export const Object_ = {
        PartnerBusiness: "partner_business",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];

    /**
     * The owner of the referred business.
     */
    export interface Owner {
        /** User ID, prefixed `user_`. */
        id: string;
        /** The user's display name. */
        name: string | null;
        /** The user's profile picture. */
        profile_picture: Owner.ProfilePicture;
        /** The user's unique username. */
        username: string;
    }

    export namespace Owner {
        /**
         * The user's profile picture.
         */
        export interface ProfilePicture {
            /** The user's profile picture URL. */
            url: string;
        }
    }

    /**
     * The referrer's commission rate for each income source, expressed as a fraction (0.3 = 30%).
     */
    export interface PayoutPercentages {
        /** Share of the referred business's Whop Ads spend. */
        ad_spend: number | null;
        /** Share of Whop's profit from card interchange. */
        card_interchange: number | null;
        /** Share of Whop's profit from product sales. */
        sales: number;
        /** Share of Whop's profit from platform balance transfers. */
        transfer: number | null;
    }

    /**
     * The second-tier partner who earns on this business (referred the first-tier partner). Null if there is no active second-tier partner.
     */
    export interface SecondTierPartner {
        /** User ID, prefixed `user_`. */
        id: string;
        /** The user's display name. */
        name: string | null;
        /** The user's profile picture. */
        profile_picture: SecondTierPartner.ProfilePicture;
        /** The user's unique username. */
        username: string;
    }

    export namespace SecondTierPartner {
        /**
         * The user's profile picture.
         */
        export interface ProfilePicture {
            /** The user's profile picture URL. */
            url: string;
        }
    }

    /** Current referral status. */
    export const Status = {
        Active: "active",
        Removed: "removed",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];

    export interface VolumeUsd {
        /** Credited GMV (awaiting_settlement + settled); excludes canceled and reversed, in USD. */
        attributed: string;
        /** GMV awaiting settlement (commission not yet computed), in USD. */
        awaiting_settlement: string;
        /** GMV of pending + completed payments, in USD. */
        settled: string;
    }
}
