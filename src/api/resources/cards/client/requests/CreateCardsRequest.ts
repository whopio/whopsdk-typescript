// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {}
 */
export interface CreateCardsRequest {
    /** The owning account ID (a biz_ identifier). Provide this or user_id. */
    account_id?: string;
    /** The account member (a user_ identifier) to assign the card to. Required for business card issuing accounts. */
    assigned_user_id?: string;
    /** A display name for the card. */
    name?: string;
    /** Spending limit amount, in dollars. */
    spend_limit?: number;
    /** The window the spend limit applies to. */
    spend_limit_frequency?: CreateCardsRequest.SpendLimitFrequency;
    /** Per-transaction limit amount, in dollars. */
    transaction_limit?: number;
    /** The owning user ID (a user_ identifier). Provide this or account_id. */
    user_id?: string;
}

export namespace CreateCardsRequest {
    /** The window the spend limit applies to. */
    export const SpendLimitFrequency = {
        Daily: "daily",
        Weekly: "weekly",
        Monthly: "monthly",
        OneTime: "one_time",
    } as const;
    export type SpendLimitFrequency = (typeof SpendLimitFrequency)[keyof typeof SpendLimitFrequency];
}
