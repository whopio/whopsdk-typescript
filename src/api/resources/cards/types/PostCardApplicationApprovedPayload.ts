// This file was auto-generated by Fern from our API Definition.

export interface PostCardApplicationApprovedPayload {
    /** The account ID that this webhook event is associated with */
    account_id?: (string | null) | undefined;
    /** The API version for this webhook */
    api_version: PostCardApplicationApprovedPayload.ApiVersion;
    /** The dated API version (Api-Version-Date) the payload is serialized to */
    api_version_date: string | null;
    data: PostCardApplicationApprovedPayload.Data;
    /** A unique ID for every single webhook request */
    id: string;
    /** For some `.updated` events, the old values of the payload fields that changed, keyed by field name. Omitted when no capture is available for the event */
    previous_attributes?: Record<string, unknown> | undefined;
    /** The timestamp in ISO 8601 format that the webhook was sent at on the server */
    timestamp: string;
    /** The webhook event type */
    type: PostCardApplicationApprovedPayload.Type;
}

export namespace PostCardApplicationApprovedPayload {
    /** The API version for this webhook */
    export const ApiVersion = {
        V1: "v1",
    } as const;
    export type ApiVersion = (typeof ApiVersion)[keyof typeof ApiVersion];

    export interface Data {
        /** URL where the applicant completes additional identity verification. */
        hosted_url: string | null;
        /** Card application ID, prefixed `ciac_`. */
        id: string;
        object: Data.Object_;
        /** The application status. */
        status: Data.Status;
    }

    export namespace Data {
        export const Object_ = {
            CardApplication: "card_application",
        } as const;
        export type Object_ = (typeof Object_)[keyof typeof Object_];
        /** The application status. */
        export const Status = {
            Approved: "approved",
            Pending: "pending",
            ManualReview: "manual_review",
            Denied: "denied",
            Locked: "locked",
            Canceled: "canceled",
            NeedsVerification: "needs_verification",
            NeedsInformation: "needs_information",
        } as const;
        export type Status = (typeof Status)[keyof typeof Status];
    }

    /** The webhook event type */
    export const Type = {
        CardApplicationApproved: "card_application.approved",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
}
