// This file was auto-generated by Fern from our API Definition.

import type * as Whop from "../../../index.js";

export interface CreateQuotePayoutsResponse {
    /** Gross payout amount. */
    amount: Whop.Money;
    /** Exact amount quoted for delivery. */
    destination_amount: Whop.Money;
    /** Quoted exchange rate from the source currency to the destination currency. */
    exchange_rate: number;
    /** When the quote expires. */
    expires_at: string;
    /** Fee charged for the payout. */
    fee: Whop.Money;
    /** Provider-backed payout quote ID, prefixed `pout_`. */
    id: string;
    /** Amount remaining after fees. */
    net_amount: Whop.Money;
    object: CreateQuotePayoutsResponse.Object_;
    /** Server-signed quote token to submit to POST /payouts. */
    quote_token: string;
}

export namespace CreateQuotePayoutsResponse {
    export const Object_ = {
        PayoutQuote: "payout_quote",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
