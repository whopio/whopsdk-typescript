// This file was auto-generated by Fern from our API Definition.

export interface RetrieveSwapsResponse {
    /** Account that owns the swap: a business ID prefixed `biz_`, or the user ID for a personal account. */
    account_id: string;
    /** Latest error returned for a failed swap. */
    error?: (string | null) | undefined;
    /** Swap ID. */
    id: string;
    object: RetrieveSwapsResponse.Object_;
    /** Current swap status. `complete` and `failed` are terminal. */
    status: RetrieveSwapsResponse.Status;
    /** On-chain transaction hashes produced by the swap. */
    tx_hashes: string[];
    /** User whose personal account owns the swap, prefixed `user_`. Null for a business account. */
    user_id: string | null;
}

export namespace RetrieveSwapsResponse {
    export const Object_ = {
        Swap: "swap",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
    /** Current swap status. `complete` and `failed` are terminal. */
    export const Status = {
        Queued: "queued",
        Working: "working",
        Complete: "complete",
        Failed: "failed",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
